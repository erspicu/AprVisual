# 工具與部署操作筆記

> 這份筆記記錄本專案各工具的實際用法與**踩過的雷**,給之後的 session / 自己參考。
> **安全鐵則:任何帳號、密碼、API key 一律不寫進這份檔案(repo 是公開的)。** 只記「放在哪」與「怎麼用」。
> 機密位置:`C:\key\config.json`(Gemini key)、`C:\key\gmail.txt`(Gmail app password)、FTP 帳密(由使用者當下提供,不留存);`workflow/`、`WebSitePHP/` 皆 gitignored。

---

## 1. 問 Gemini — `tools/knowledgebase/gemini_query.py`

- **Key**:讀 `C:\key\config.json`(含 `api_key` 與 `model` 兩欄)。
- **基本用法**(prompt 很長時一律用檔案,避免 argv 長度/編碼問題):
  ```powershell
  python tools/knowledgebase/gemini_query.py -f <prompt檔> -o <輸出檔>
  ```
  - `-f / --prompt-file`:從 UTF-8 檔讀 prompt。
  - `-o / --output`:把回答另存。
  - `-l / --list-models`:列出可用模型,寫到 `models_list.txt`。
- **自動 log**:每次問答都會存到 `tools/knowledgebase/message/<時間戳>.txt`(問題 + 回答)。這些 log 未進版控。
- **網路**:要實際連外,跑指令時用 `dangerouslyDisableSandbox: true`,timeout 拉長(回答可能要 1–2 分鐘)。
- **問 hot-path / 演算法問題的鐵律**(很重要):prompt 一定要附上**已知死路清單 + L1d 預算 + 94% 雙向 SCC** 等結構事實,否則 Gemini 會一直重複建議我們早就實測失敗的東西(IR、codegen、GPU、bitset、平行化…)。範本見 `temp/gemini_prompt_algo.txt`。

---

## 2. 寄信 — `tools/send_mail.py`

- **管道**:Gmail SMTP over SSL(`smtp.gmail.com:465`)。
- **密碼解析順序**:`--password` 參數 → `C:\key\gmail.txt` → 環境變數 `GMAIL_APP_PASSWORD`。
- **預設收/寄件**:都是 `baxermux@gmail.com`(可用 `--to` / `--from` 覆寫)。
- **常用**:
  ```powershell
  # 純文字
  python tools/send_mail.py --to EMAIL -s "主旨" -b "內文"
  # 從檔案(.md/.txt/.html),Markdown 轉漂亮 HTML 信
  python tools/send_mail.py --to EMAIL -s "主旨" -f body.md --html
  # 帶附件(圖、zip…可多個)
  python tools/send_mail.py --to EMAIL -s "主旨" -b "內文" -a a.png b.zip
  # 先看不送
  python tools/send_mail.py --to EMAIL -s "主旨" -b "內文" --dry-run
  ```
- `--html` 會用內建(無外部相依)的簡易 Markdown→HTML,支援標題/表格/清單/程式碼區塊/引用。
- **習慣**:送信屬於對外動作,送之前先 `--dry-run` 確認內容,真的要送再拿掉。

---

## 3. 部署 PHP 排行榜後端到 baxermux.org(FTPS)

站台後端 `WebSitePHP/`(gitignored)是手動 FTP 部署到 `baxermux.org` 的;GitHub Pages 的 `WebSite/` 才是走 git。

**踩過的雷(全部都遇過,記起來)**:
1. **帳號要用 `帳號@網域` 格式**(例如 `user@baxermux.org`),只給 `user` 會 530 登入失敗。
2. **主機強制 FTPS(explicit TLS)**:純 FTP 會被拒(530)。要加 `--ssl-reqd`;憑證可能不完全匹配,加 `-k`(insecure)略過驗證。
3. **密碼有特殊字元(`{ # ) ~ (` …)**:不要直接放在指令列,會被 shell 吃掉。用 **curl 設定檔 `-K`** 傳帳密(設定檔裡用雙引號包住 `user = "帳號:密碼"`)。
4. **PowerShell 的 `curl` 是 `Invoke-WebRequest` 的別名**,FTP 上傳要明確呼叫 `curl.exe`。
5. 要實際連外 → `dangerouslyDisableSandbox: true`。

**列目錄(先確認連線與位置)**:
```powershell
$cfg = Join-Path $env:TEMP 'ftp.cfg'
[IO.File]::WriteAllText($cfg, ('user = "帳號@網域:密碼"' + "`n" + 'ssl-reqd' + "`n" + 'insecure'))
curl.exe -sS --connect-timeout 25 -K $cfg "ftp://baxermux.org/public_html/myemu/AprVisual/"
```
**上傳單一檔(只傳改過的)**:
```powershell
curl.exe -sS -K $cfg -T <本機檔> "ftp://baxermux.org/public_html/myemu/AprVisual/<遠端檔名>"
```
**鐵則:絕不覆蓋 `config.php`(站台機密設定)與 `data/`(SQLite 排行榜資料庫)。** 只傳真正改動的程式檔(例如改 CORS 只傳 `lib.php`)。傳完用 curl 列目錄看大小/時間戳確認,並去打 API 驗證(見下)。

---

## 4. 排行榜 API(供官網/換算器抓資料)

- 端點:`https://baxermux.org/myemu/AprVisual/api.php`
- 取第一名:`GET ?action=list&mode=top` → 回傳 `{ ok, rows: [...] }`,`rows` 已依 `hc_per_sec` 由高到低排序,**`rows[0]` 就是第一名**。
- 欄位:`hc_per_sec, engine(csharp/rust), engine_version, commit_date, cpu_model, country, uploaded_at(ISO), checksum, …`。
- **CORS**:`lib.php` 的 `json_out()` 已加 `Access-Control-Allow-Origin: *`,所以 GitHub Pages(跨網域)可以 `fetch`。改這支務必重新 FTP 上傳才會生效。
- 驗證 CORS 是否上線:
  ```powershell
  (Invoke-WebRequest 'https://baxermux.org/myemu/AprVisual/api.php?action=list&mode=top').Headers['Access-Control-Allow-Origin']
  ```

---

## 5. GitHub Release(`gh` CLI)

```powershell
gh release list --repo erspicu/AprVisual --limit 30                 # 列所有發佈版
gh release view <tag> --repo erspicu/AprVisual --json body,assets   # 看說明/資產
gh release download <tag> --repo erspicu/AprVisual --pattern AprVisualBenchMark.zip --dir <資料夾> --clobber
```
- **版本 ↔ 引擎 commit 對照**寫在 release 的 body(例:`benchmark-2026.06.04` = 引擎 `b1699b0`、`benchmark-2026.06.05` = `0d15eed`)。要拿「某 commit 的 benchmark」就用對應的 release tag 下載,不一定要自己 build。
- 連外 → `dangerouslyDisableSandbox: true`。

---

## 6. 打包發佈 benchmark — `tools/release_benchmark.ps1`

```powershell
pwsh tools/release_benchmark.ps1 -Version <日期> -Publish
```
- C# 出多檔 self-contained trimmed(不是單一 exe);同時含 C# 與 Rust、Windows + macOS。
- 發完記得更新 `WebSite/index.html` 的下載連結與版本行。

---

## 7. 效能 A/B 測試

**前提**:熱路徑 memory-latency-bound,絕對值隨 CPU 時脈/boost 漂移很大。要比 sub-1% 改動,**先鎖頻**(見 §8),再用 interleaved-paired(交錯配對、取中位數)。

- **比兩個原始碼版本(dotnet dll)**:`tools/ab_bench.ps1`
  ```powershell
  pwsh tools/ab_bench.ps1 -BaseDir temp/bench_base -ExpDir src/AprVisual.S1/bin/Release/net10.0 -Rounds 20
  ```
- **比兩個發佈版(self-contained exe)**:手寫交錯迴圈,每回合各跑一次 base/exp,抓輸出的 `(NNN hc/s)` 與 `checksum @ t=...`,報中位數 / 修剪平均 / 配對勝場。執行檔在 `<解壓目錄>\win\csharp\AprVisual.S1.exe`(C#)與 `<解壓目錄>\win\wire_s1.exe`(Rust);參數參考 `run_csharp.bat`:`--benchmark roms\full_palette.nes --bench-hc 300000 --extra-ram --system-def-dir data\system-def --log-dir log`。
- **黃金 checksum**:`0x794A43ABDF169ADA`(full_palette 300k --extra-ram)。每次都要確認 bit-exact。
- 熱身那一輪(第一次跑)丟掉不算。

---

## 8. 鎖定 CPU 時脈(量測穩定用)— `tools/cpu_lock_3.6ghz.bat` / `cpu_restore.bat`

- 這兩支**不進版控**(使用者要求),放 `tools/`。
- 透過 OS 層 `powercfg`(關 boost + `PROCTHROTTLEMIN/MAX 100`)把 CPU 壓在固定基頻;`cpu_restore.bat` 還原。
- 需系統管理員權限;`.bat` 必須是 **CRLF** 換行(LF 會出現 `'M' 不是內部或外部命令` 的 parse 錯誤)。
- 記憶體時脈是 BIOS/POST 階段的事,執行期改不了(DOCP/XMP 在 BIOS 設)。
- 鎖頻後 C# 約 ~76.5K、boost 約 ~91K —— 「覺得變慢」多半是拿單次 boost 高讀數在比,鎖頻取中位數才準。

---

## 9. Git commit / push 慣例與雷

- **規則**:每完成一個階段就 `git add` + `git commit -F <檔>` + `git push` 一次串起來(使用者要求 commit 後一定 push)。
- commit 訊息結尾固定加:`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- **雷 1:多行 commit 訊息**別用 `git commit -F -` 配 here-string 直接接在分號後(不會被導進 stdin,會被當 pathspec)。正解:用 PowerShell here-string `@'...'@` 寫進 `$env:TEMP` 的暫存檔,再 `git commit -F $f`。
- **雷 2:路徑防呆誤擋**。若同一條 PowerShell 指令裡同時出現 `Remove-Item` 和字串內的 `/`(例如 commit 訊息裡有 `EN/ZH`、`3-6x`),會被守門判成「刪系統路徑」而整條被擋。正解:把 `Remove-Item`(清暫存檔)那步**拆成另一條指令**,或乾脆別清(暫存檔無妨)。
- **雷 3:cwd 會漂**。Bash 工具的工作目錄會留在上次 `cd` 的地方,導致 `dotnet publish src/...` 找不到專案。用絕對路徑或 `git -C C:\ai_project\AprVisual ...`。

---

## 10. 不要碰 / 不要外洩

- `ref/`:gitignored、唯讀參考(授權資料/第三方),不要改、不要把內容複製出來。
- `src/AprVisual.Deprecated/`:唯讀,除非明確要求否則不動。
- `WebSitePHP/`、`workflow/`:gitignored(含 FTP 帳密、站台機密),**永遠不要 commit**。
- `C:\key\*`、FTP 帳密:不寫進任何會進 repo 的檔案。
- 對外動作(寄信、FTP 上傳、發佈)做之前先確認;送出即公開,可能被快取/索引。
