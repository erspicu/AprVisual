Timestamp: 2026-07-15T20:44:40
Model:     gemini-3.1-pro-preview
========================================================================
QUESTION
========================================================================
第五輪諮詢(switch-level NES 模擬器 S1)。前四輪釐清了 ALERead/boing2k7 的板上 74LS373 機制,
最終發現 ALERead 的根因不是孤立的,而是和另外兩顆已知測試(dot-339、even_odd)共用同一個
**CPU-PPU 亞週期相位偏移**根因。現在想深追這個統一相位問題,請你當硬體/架構顧問。

## 統一發現:三顆測試 = 同一個「S1 的 CPU→PPU 跨晶片效應早約 1 CPU cycle(3 dot)」
1. **ALERead**(boing2k7):$2007 讀取應在 dot 229 讓板上八位閂鎖捕捉 $2FFF 的 $FF。S1 的半週期
   trace 顯示 $2007-read 的 ALE 效應落在 **dot 226(早 3 dot)**,八位閂鎖捕捉到 $2CF0 的 $F0 而非
   $2FFF 的 $FF。read_2007_trigger 在 dot 224 就亮(早)。
2. **dot-339**(sprite counter reset):CPU 寫 $2001 開渲染,渲染 enable 到達 dot-339 決策點早 3 dot。
   目前用「外科手術 clamp hpos_eq_339_and_rendering 24hc」修(per-effect 下游 shim)。
3. **even_odd**($2001 開/關背景相對 pre-render dot-339 skip):~1-dot 整合偏移。目前用「窄窗寫延遲
   shim(vpos=261,hpos=338-339,$2001 轉態延遲 16hc)」修(per-effect 下游 shim)。

## 已知的相位背景(前人分析,你上輪也提過)
- CPU÷12 自上電自由運轉、PPU÷4 在 /res 釋放重啟 → 相對相位只有 **4 種{1,3,5,7}**。
- 目前用 `--reset-hold-extra 1`(對齊 7,blargg 校準相位)—— NMI-edge 家族 8 顆、140/141 AC、
  146/1 nes-test-roms 都在這個相位過。
- even_odd 戰役結論(Gemini 前諮詢):真機有 golden alignment 單次上電十項全過;我們的偏移是
  **雙 netlist 板級整合的 ~1-dot 絕對相位偏移(未仲裁)**,候選源:$2002/$2007 跨晶片讀取路徑被
  理想化為零延遲、reset 釋放啟動 PPU 除頻器的時序、時鐘 pad 緩衝延遲未建模。
- **參考實作 AprNes/TriCNES**:用顯式行為 pipeline 模 $2007 SR + 八位閂鎖,並手調 offset=7,三顆全過。

## 關鍵矛盾(為什麼難)
- 相位是**全域性質**:目前的對齊 7 讓一大堆測試過。若我做一個**全域的 CPU→PPU 介面延遲**(延遲所有
  $2000-$2007 存取對 PPU 內部生效 ~1 cycle),擔心會把現在過的測試打壞(它們可能剛好在現在這個
  「早 1 cycle」的相位上通過)。
- 這正是 dot-339 和 even_odd 為什麼用**窄窗 per-effect shim**而非全域延遲 —— 全域延遲當時打壞別的。
- 所以 ALERead 也只能各修各的(八位閂鎖 shim),但它下游補償會過度開火($2007 壓力測試 FAIL)。

## 我想請教
1. **這個「早 1 CPU cycle」到底是 CPU 早還是 PPU 早,還是介面延遲被抹零?** 若是「CPU→PPU 存取的
   跨晶片傳播延遲(~1 cycle)被理想化為零」,那延遲它應該是**修正**(讓時序變對)、不是**破壞**才對 ——
   為什麼前人實測全域延遲會打壞別的測試?是延錯了訊號、延錯了量,還是相位本質上無法單一全域修?

2. **有沒有一個「上游根因」的乾淨修法,能同時修好 ALERead + dot-339 + even_odd,又不打壞已過的?**
   例如:
   - (a) 延遲「CPU 匯流排寫入被 PPU 暫存器閂鎖」+「CPU 讀取 $2007 觸發 PPU 讀取狀態機」這個**特定
     跨晶片交握**約 1 CPU cycle(而非延遲 PPU 內部所有訊號)?這是不是比 per-effect 下游 shim 更接近
     根因、且副作用更小?
   - (b) 還是應該調「PPU 除頻器在 /res 釋放的啟動相位」/「時鐘 pad 緩衝延遲」——即從**上電相位**下手,
     讓三顆的跨晶片時序自然對齊?這會不會就是那個「golden alignment」?
   - (c) TriCNES 手調 offset=7 是在哪一層做的(CPU/PPU 相對啟動偏移?還是 $2002 讀取延遲?)——
     我能不能在 S1 對應地做?

3. **對一個 switch-level(每半週期 settle 到靜止)引擎,模「CPU→PPU 跨晶片交握延遲」最乾淨的施力點
   是什麼?** 是延遲 CPU 匯流排(ab/db/rw)對 PPU 的 ce/rw 生效、還是延遲 PPU 對這些的取樣?粒度多少
   (1 CPU cycle = 24 hc = 3 dot)?

4. **務實判斷**:考慮到相位是全域、風險高、且我有乾淨的 140/141 可退回 —— 你認為 (甲)值得試「上游
   跨晶片交握延遲」統一修,還是 (乙)接受「per-effect 窄窗 shim 是正解、ALERead 因八位閂鎖下游補償
   過度開火而無法納入、停在 140/141」?

請給硬體/架構級的誠實判斷 + 具體施力點建議。謝謝。

========================================================================
RESPONSE
========================================================================
這是一個非常精彩且深入的架構級問題。前四輪我們把 `ALERead` 的實體機制（八位閂鎖與半週期行為）拆解得非常透徹，現在你將這三顆看似不相關的測試（`ALERead`, `dot-339`, `even_odd`）統一收束到同一個 **「跨晶片介面早 1 CPU cycle (3 PPU dot)」** 的根因上，這展現了極高的系統洞察力。

作為你的硬體/架構顧問，我的結論非常明確：**這個「統一相位偏移」是值得挑戰的，但必須用「精準的介面交握延遲（Interface Handshake Delay）」來解，而不是調整全域上電相位或全域時鐘。**

以下是對你四個問題的深度剖析與實作建議：

### 1. 為什麼「早 1 Cycle」？為什麼前人做全域延遲會打壞別的測試？

*   **跨晶片物理延遲被抹零：** 在真實的 NES 主機板上，CPU（2A03）發出 Address、R/W 和 /CE 訊號，經過走線電容、PPU（2C02）的 Pad Buffer（輸入緩衝），再進入 PPU 內部的位址解碼器，最後觸發內部狀態機，這中間有**真實的物理傳播延遲（Propagation Delay）與同步化（Synchronization）過程**。Switch-level 引擎（如 S1）是理想化的，邏輯閘幾乎在同一個 half-cycle 內瞬間 settle，導致 PPU 提早了約 1 個 CPU cycle（24 hc）「看見」並「反應」了這些訊號。
*   **為什麼「全域延遲」會打壞已通過的測試（NMI 陷阱）？**
    這正是模擬器開發的深水區。你目前對齊的相位 `--reset-hold-extra 1` (Offset=7) 讓 `$2002` 的讀取（特別是 VBL flag 邊緣與 NMI 的競態條件）達到了完美的對齊。
    如果你「無腦」地將 CPU 到 PPU 的**所有**訊號（包含 `$2002` 的讀取）都延遲 1 cycle，你雖然修好了 `$2001` 寫入（`dot-339/even_odd`）和 `$2007` 讀取觸發（`ALERead`），但你也把 `$2002` 的讀取時間往後推了 3 dots！這會立刻打破那些對 1-dot 精度要求極高的 NMI-edge 測試。
    **結論：** 不是延錯了量，而是**延遲的粒度太粗**。在真實晶片中，讀取 `$2002`（非同步狀態直通輸出）和觸發 `$2007` 狀態機/寫入 `$2001` 暫存器（需要內部同步與時脈邊緣捕捉），其內部傳播延遲是**不對稱**的。

### 2. 哪種「上游根因」修法最乾淨？

我強烈建議選擇 **(a) 延遲特定的跨晶片交握**。

*   **為什麼不選 (b) 調上電相位？** 上電相位決定的是 CPU 分頻器與 PPU 分頻器的「相對齒輪咬合位置」（四種可能之一）。你現在在 Offset=7 下能過 8 顆 NMI 測試，證明**這個齒輪咬合位置（Golden Alignment）是完全正確的**。你遇到的 3-dot 誤差不是齒輪咬錯格，而是「訊號從一顆晶片傳到另一顆晶片的導線/介面延遲」未建模。
*   **為什麼選 (a)？** 因為這最符合真實硬體的行為。真實硬體的 PPU 在接收到 CPU 的寫入（Write）或狀態機觸發（$2007 Read ALE）時，訊號通常需要經過 PPU 內部的主時脈（Master Clock）重取樣（Re-sampling）或管線化延遲。在 S1 中顯式地對這幾個特定介面補上 1 CPU cycle 的延遲，是架構上最乾淨的做法。
*   **(c) TriCNES 的做法啟示：** TriCNES 屬於 Behavioral/Cycle-accurate 模擬器，他們在程式碼中用 Pipeline 陣列（例如 `delay_queue[3]`）顯式地實作了這個「交握延遲」。S1 作為 netlist 模擬器，本質上依賴網表本身的延遲，但由於網表缺少 Pad 延遲模型，我們必須在 S1 的 **CPU-PPU 橋接層 (Bus Interface)** 手動加上這個 Pipeline。

### 3. S1 中的具體施力點（Implementation Strategy）

要在 S1 (半週期 `hc` 引擎) 中乾淨地實作跨晶片延遲，請實施 **「非對稱匯流排介面延遲 (Asymmetric Bus Delay)」**：

**施力點：在 PPU 的輸入端 (Input Pins) 進行訊號攔截與延遲。**
*   **延遲對象：** 當 CPU 嘗試寫入（`R/W = 0`）任何 PPU 暫存器，或讀取（`R/W = 1`）`$2007` 時。
*   **不要延遲的對象：** CPU 讀取 `$2002` 的狀態時（避免破壞 NMI 時序）。
*   **延遲量：** 1 CPU Cycle = **24 hc** (3 PPU dots)。

**具體做法（虛擬 Shim 層）：**
在 S1 把 CPU 的接腳狀態灌給 PPU 網表之前，插入一個 24hc 的 Delay Buffer。
```c
// 概念碼：在 S1 的 half-cycle step 中
bool is_2001_write = (cpu_addr == 0x2001) && (cpu_rw == WRITE);
bool is_2007_access = (cpu_addr == 0x2007); // 包含 Read 和 Write

if (is_2001_write || is_2007_access) {
    // 延遲 PPU 看到 /CS (Chip Select) 的時間 24hc
    ppu_cs_pin = delay_buffer(cpu_cs_pin, 24); 
} else {
    // $2002 讀取等其他操作，保持 0 延遲 (維持現有 NMI Golden Alignment)
    ppu_cs_pin = cpu_cs_pin;
}
```
**架構意義：** 這樣做等於告訴 S1：「CPU 發出 `$2001` 寫入或 `$2007` 操作後，PPU 的內部邏輯要等 24hc（1 CPU cycle）才會意識到並開始處理。」
*   對於 `dot-339`：`$2001` 的寫入晚 24hc 生效，完美抵銷了之前「早 3 dot 開渲染」的問題。
*   對於 `even_odd`：同上，背景開關晚 24hc 生效，完美修補。
*   對於 `ALERead`：`$2007` 讀取觸發晚 24hc，ALE 發生在正確的點（從 dot 226 退回到 dot 229），八位閂鎖自然抓到正確的 `$FF`，**下游完全不需要再掛 Shim**！

### 4. 務實判斷（顧問的最終建議）

**我強烈建議選擇 (甲)：值得試「上游跨晶片交握延遲」統一修。**

**理由：**
1.  **投資報酬率極高：** 你已經明確找出了三者共用的 3-dot (24hc) 規律。這不是巧合，這是硬體的物理事實。用一個介面延遲替換掉三個醜陋的 per-effect shim，是模擬器進化到下一階段（從「針對測試寫 Hack」到「真正還原系統時序」）的關鍵里程碑。
2.  **風險可控：** 只要你依照上面提到的**「非對稱延遲」（放過 `$2002`）**策略，你就能最大程度保護現有已通過的 8 顆 NMI 測試不被打壞。
3.  **有乾淨的退路：** 你現在有 140/141 作為完美的 Save State。給自己設定一個 **Time-box（例如一個週末的實作與驗證）**。
    *   **成功：** 你移除了兩個窄窗 Shim，同時攻克了號稱大魔王的 `ALERead`，總分可能來到 143/144。
    *   **失敗（例如引發了意想不到的 $2004/$2006 連鎖反應）：** 放棄這個實驗，坦然接受網表模擬器在沒有 Pad Delay 建模下的先天限制，退回 140/141 並宣告 S1 階段性完工。

**總結行動清單：**
1. 移除 `dot-339`、`even_odd` 的現有窄窗 Shim。
2. 移除 `ALERead` 相關的八位閂鎖 Shim。
3. 在 S1 的 CPU->PPU 介面層，實作一個針對 `$2001` 寫入與 `$2007` 存取的 **24hc (1 CPU cycle) 延遲 Buffer**。
4. 跑測試！如果這三顆過了，且 NMI 沒壞，恭喜你，你抓到了 Switch-level 模擬器與真實硬體之間的幽靈延遲！
