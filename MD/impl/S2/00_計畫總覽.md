# S2 — netlist → 邏輯 IR：計畫總覽

> 對應 `MD/struct/08` 四階段的 **S2**。S1（含 S1.5 lowering）已完成 —— 我們有一個整片板子的開關層級模擬器當 golden reference。S2 的任務：**從那張電晶體網表抽取出「每個 node 的 next-state 布林表達式（`Expr` IR）」**，用一個 IR 直譯器執行它，行為要跟 S1 開關層級直譯器**逐 node、逐 cycle 完全一致**。
>
> 本檔已經過 Gemini（`gemini-3.1-pro-preview-customtools`）一輪整體 review，採用的建議直接寫進下面的設計（見 §7 review 紀錄；log 在 `tools/knowledgebase/message/20260512_100205.txt`）。

---

## 0. 這份文件 & 文件結構

- **本檔（`00_計畫總覽.md`）** = S2 的總覽：核心問題、我設計的步驟拆解、每個 step 的工作流程（含 Gemini review 迴路）、等價驗證紀律、過關條件、不在 S2 範疇的東西、參考資料、Gemini review 紀錄、進度日誌。
- S2 有多個 stage，**一個 stage 一個檔**：每個 stage 開工時建 `MD/impl/S2/0N_S2.N_<名稱>_設計.md`，內含：
  1. **設計描述** —— 我構想的處理方式 / 資料結構 / 演算法（要先寫好才能丟 Gemini）。
  2. **Gemini 分析回饋** —— 用 `tools/knowledgebase/gemini_query.py -f <prompt檔>` 把設計描述丟給 Gemini，原文記下它的分析 + 建議。
  3. **建議評估 & 採用** —— 逐條評估，**有正面性幫助的一律採用、寫進實作**；不採用的也記為什麼。
  4. **實作筆記** —— 實際做了什麼、踩到什麼、跟原設計的偏離。
  5. **驗證結果** —— 等價 gate、覆蓋率、效能對比的數字。

## 0.1 範疇（依 `MD/struct/08` 2026-05-12 路線圖調整）

- **整片板子的網表**：2A03（CPU）+ 2C02（PPU）+ 板上 74xx 膠合（74LS373/139/368、74HC04）+ CIC（nes-cic1）+ 手把（nes-pad）+ NROM mapper 邏輯（nes-cart72 / cart-mmu0）。**不是「先 CPU 後 PPU」** —— 抽取是一套演算法跑整張圖；人為把 CPU/PPU 隔開反而要設計「IR 區 ↔ switch-level 區」的跨區協定（= 失敗點 #1 的溫床）。
- **記憶體陣列維持行為級**（u1/u4 SRAM 的 2KB、cart PRG-ROM/CHR-ROM/CHR-RAM、`$6000` 工作 RAM）—— 它不是電晶體網表，是 `byte[]` + handler，是**模型的邊緣**；S2/S3/S4 都一樣 call out 到那個陣列。S2 不對它抽邏輯（它沒邏輯）。
- **「分階段」按結構複雜度，不按晶片**：先抽良性結構（反相器 / NAND / NOR / 串並聯下拉網路 / 明確的 precharge-evaluate）→ 那一刀就 cover 晶片大部分 → 高覆蓋率 IR + 少數還沒處理的區域 fallback 回 switch-level（hybrid）→ 隨著處理更多結構類型，覆蓋率往 100% 爬。「最難的結構」（shift register / OAM DRAM / 共享 bus / 強耦合 dynamic）剛好大多落在 2C02。
- 只 NROM（mapper 0）—— 跟 S1 一致。

---

## 1. 核心問題：怎麼從「開關層級網表」抽出「邏輯」？

開關層級模型在每個瞬間做的事：給定所有 gate node 的狀態 → 整張網表依「目前導通的電晶體」分成連通群 → 每群的值 = `resolve(群裡所有 node 的 flags OR 起來)`（優先鏈：GND 贏 → VCC/pull-up → 外部驅動 → 保留前值 → 0；純浮島：capacitance 最大的 node 勝）。

「抽出邏輯」= 把這整套塌成「`next_state[每個 node] = f(current_state[每個 node], 外部輸入)`」，並把 `f` 表達成布林電路（`Expr` DAG）。對 NMOS 電路，`f` 的結構**大部分**很乾淨：

### 1.1 良性結構（佔晶片大部分）—— 「下拉網路 + 負載」

標準 NMOS 邏輯閘 = 一個「**下拉網路**」（電晶體串並聯接到 GND）+ 一個「**負載**」（depletion-load 弱拉高 / pull-up 接 VCC）。閘輸出 node `N`：

```
N = NOT( 下拉網路導通 )
下拉網路導通 = 對 gate signals 的布林式（串聯 = AND、並聯 = OR）
```
- 反相器：`N = NOT(a)`；NAND：`N = NOT(a AND b)`；NOR：`N = NOT(a OR b)`；AOI/OAI：`N = NOT((a AND b) OR c)`…
- 把「下拉網路」當「N→GND、邊權是 gate signal 的圖」做**串並聯歸約**得到布林式；歸不出純串並聯（橋接結構）→ 退一步用「N→GND 的路徑 OR、每條路徑上 gate 的 AND」；路徑數爆炸 → 標 hybrid。

### 1.2 難 / 需要小心的結構（少數、大多在 2C02）

- **pass transistor / transmission gate —— 而且雙向**：NMOS 的 pass transistor**沒有方向**；S1 是靠「誰的驅動強度大」決定 A 驅動 B 還是 B 驅動 A。所以「純 pass fan-in = Mux」這個假設不夠 —— **S2.1 要做方向性推導**（典型：A 側有 PULLDOWN 網路、B 側純 dynamic（只有 pass 連進來）→ 方向 A→B）；推不出單一方向的 pass 網路（如 2C02 OAM 讀寫匯流排）→ **不要硬轉 Mux，直接 hybrid**。能定向的 → `N = Mux(en, M, Hold(N))`，多顆 → mux 鏈。
- **active pull-up / superbuffer（push-pull）**：6502 的 clock generator / address pin 等用一顆 depletion NMOS 當 *active* pull-up（gate 接信號而非短路到 source）→ 形成 push-pull。這種 node：`N = Mux(pullup_cond, 1, Mux(pulldown_cond, 0, Hold(N)))`，並 assert `!(pullup_cond AND pulldown_cond)`。→ S2.0 的電晶體分類要能辨識 `ACTIVE_PULLUP`（vs 接 VCC 的 strong pull-up vs 接自己 source 的 static depletion load）。
- **precharge-evaluate（domino logic）**：2C02 sprite 邏輯大量用 —— phase1 透過（conditional / clocked）pull-up 預充至 1，phase2 下拉網路求值。Expr 長 `N = Mux(phi1, 1, NOT(pulldown_eval))`。落在現有設計範圍內，但 **S2.1 要把「預充用的 pull-up」辨識成 conditional pull-up，不是 static load**。
- **dynamic node（電容存值）**：沒有下拉路徑也沒 pull-up，只在某些 phase 被 pass 進來、其他時候靠寄生電容保持 → `next[N] = (被驅動) ? 驅動值 : prev[N]` = `HoldExpr`。
- **回授環（cross-coupled latch）**：兩個 node 互相當對方下拉網路的一部分 → SCC → 序向元件（register bit、SR latch）→ `next = sel ? data : prev`，指回上一輪狀態（`NodeRefExpr`/`prev[...]`）。CPU/PPU 裡幾乎每個 register bit 都是這種，而且**靠 pass transistor 隔離 + 兩相時鐘**（見 S2.3）。
- **charge sharing（電荷分享）**：兩個 capacitance 不同的 dynamic node 經 pass 相連、沒 VCC/GND → 強電荷覆蓋弱電荷（Visual6502 有 node area capacitance）。6502 真正靠這個決定邏輯值的場景極少（多半只在預充放電）。**初期：兩端都 floating 的 pass 導通 → 標警告 + hybrid**，看 test ROM 有沒有真依賴。
- **共享 / tri-state bus**：多驅動者、可能 tri-state → 勝負由 *driving strength* 決定（MOSSIM 的 signal strength）。但我們的 `Expr` IR 是**純布林、沒有 strength 維度** → bus-fight 區域 → **hybrid**。

### 1.3 結論

「抽邏輯」= 對每個 node 算「驅動結構」（下拉式 / pull-up 種類（static load / VCC strong / conditional / active）/ pass 進來的分支 + 方向），把良性的塌成 `Expr`，難的標 hybrid 或丟 SCC 分析，最後得到「組合邏輯 DAG + 序向 node（clock edge 更新）+ 少量 hybrid 區」的 IR。

### 1.4 等價驗證的兩個前置決策（X / floating / 初始化）

開關層級在剛開機時 node 可能 floating / 未定，要幾個 cycle 才全部初始化。為了不為這個吃苦：

- **不在 IR 引擎裡做三值邏輯（X）**。理由：S1 的 `NodeStates` 本來就是 `byte` 0/1（不追 X）—— power-on 是 `Reset()`（pull-up→1、其餘→0）+ `RecomputeAllNodes()` 把網表 settle 到一個**決定性**的穩態。所以根本沒有「S1 那邊是 X」的對齊問題。
- **IR 引擎的初始 state = S1 settle 完的 power-on state**：IR 引擎跟 S1 **共用同一個 `NodeStates` 陣列**，`ResetNes(full:true)` 跑完（含 `RecomputeAllNodes()` 的 S1 settle）後 IR 引擎接手 —— 自動從 S1 的穩態開始。等價檢查就是「IR 引擎從這個穩態演化 vs S1 引擎從同一穩態演化，逐 half-cycle 比 node-state 陣列」。
- **`HoldExpr` 語義必須對齊 S1**：當 S1 某 node 失去所有驅動 → 靠 capacitance 保持前值（再被導通進來的群覆蓋）。`HoldExpr(N)` 的行為要跟 S1 「保留前值 → 被群覆蓋」的優先鏈完全一致（這也是為什麼 charge-sharing 那種「強覆蓋弱」要 hybrid —— 純 `prev[N]` 表達不了）。
- （備案）若這個簡化在某些區域對不齊：選項 B = 前 N 個 reset cycle 關閉逐 node 檢查、把 S1 的 state array 整個蓋給 IR 當初值、從穩態開始對；選項 A = IR 真的上三值邏輯（`Expr` eval 支援 X 傳播）。先不做，撞到再說。

---

## 2. 我設計的 S2 步驟拆解

> 編號 S2.0～S2.6（S2.3 拆成 a/b）。每一步都有自己的設計檔 + Gemini review + 驗證。S2.4（IR 直譯器）之前的 step 都不改 S1 行為（只是建分析資料 / 平行 backend）。

### S2.0 — 靜態圖 + 角色標記（無邏輯抽取）

**做什麼**：從 lowered netlist（`_nodes`/`_transistors`，整片板子）建分析用的 `NetlistGraph`：
- **node 角色** `NodeRole`：`SUPPLY`（vcc/vss）/ `INPUT`（從不被網表驅動、只被 handler·reset `SetHigh/Low` 的 —— 至少 `clk`、`res`）/ `BUS`（雙向：既被網表又被 handler 驅動，如 `db[]`、PPU/RAM 資料線）/ `INTERNAL`（其餘）。
- **電晶體分類** `TransistorKind`：`STATIC_LOAD`（depletion load，gate 接自己 source 或 weak 旗標）/ `STRONG_PULLUP`（channel 端是 VCC、gate 永遠 1）/ `ACTIVE_PULLUP`（depletion NMOS、gate 接信號 → push-pull 的上半）/ `PULLDOWN`（channel 端是 GND，或經串聯鏈到 GND）/ `PASS`（兩 channel 端都是信號 node）。
- **每顆 PASS 電晶體上預留 `Direction` 欄位**（`Unknown` / `A_to_B` / `B_to_A`），S2.1 推導後填。
- **共用 `NodeStates`**：確認 IR 引擎之後跟 S1 共用那個 `byte*` 陣列（power-on 的 X 問題見 §1.4）。

**資料結構**：`src/AprVisual/Sim/Logic/NetlistGraph.cs`（`Logic/` 子目錄）；沿用 build-time 的 `Node.Gates`/`C1c2s`，外掛角色 / kind / direction 的平行陣列。
**驗證**：count 各角色的 node、各 kind 的 transistor；spot-check（vcc/vss=SUPPLY、clk/res=INPUT、`cpu.db0`=BUS、某個確定是 depletion load 的=STATIC_LOAD）。**不影響模擬行為**。

### S2.1 — 導通分析（每個 node 的「驅動結構」+ pass 方向）

**做什麼**：對每個非 INPUT/SUPPLY node `N`，算 `DriveInfo`：
- `pullDown : Expr?` —— 「N 被拉到 GND」的布林條件（沿 PULLDOWN 子圖做以 N 為源、GND 為匯的串並聯歸約；非串並聯橋接 → 退 path-enumeration 設爆炸上限；超限 → `Complex`/hybrid）。
- `pullUp` 種類：`StaticLoad` / `StrongVcc` / `Conditional(Expr)`（如 precharge 的 clocked pull-up）/ `Active(Expr)`（superbuffer）/ `None`。
- `passes : List<(int otherNode, Expr cond, Direction dir)>` —— 透過 PASS 電晶體連到別的信號 node 的分支；**方向性推導**：A 側有 PULLDOWN 而 B 側純 dynamic → A→B；兩側都有 driver / 推不出 → 整個 pass 網路 → hybrid（不硬轉 Mux）。

**演算法**：PULLDOWN 子圖串並聯偵測（series 鏈 / parallel 分支；橋接 → path enum + 上限）；PASS 電晶體單獨收集 + 方向推導 heuristic（參考 perfect6502 `netlist_to_c.py` 的 pass-grid 化簡，見 §6）。
**輸出**：`DriveInfo[nodeId]`（含 hybrid 標記）。
**驗證**：spot-check 已知閘 —— 某反相器 node：`pullDown == NodeRef(gate)`、`pullUp == StaticLoad`、`passes` 空；某 NAND：`pullDown == And(a,b)`；某 register bit 的 cross-coupled pass 對：方向推不出 / 互指（交給 S2.3）。

### S2.2 — `Expr` IR + 良性結構抽取

**做什麼**：
- `Expr` 型別：`ConstExpr(bool)` / `NodeRefExpr(int)`（讀現值）/ `PrevExpr(int)`（讀上一輪值，由 S2.3 打斷環時引入）/ `NotExpr` / `AndExpr` / `OrExpr` / `MuxExpr(cond,a,b)` / `HoldExpr(int)`。
- 由 `DriveInfo` 組 `nextExpr[N]`：
  - 下拉 + static/strong pull-up、無 pass → `Not(pullDown)`（反相器/NAND/NOR/AOI）。
  - 有 active pull-up + 下拉 → `Mux(pullup_cond, 1, Mux(pulldown_cond, 0, Hold(N)))`，並記下 assertion `!(pullup_cond AND pulldown_cond)`（執行期可選擇性檢查）。
  - 有 conditional（clocked）pull-up（precharge）→ `Mux(precharge_cond, 1, Not(pulldown_eval))`。
  - 純 pass fan-in（已定向）→ `Mux(cond1, M1, Mux(cond2, M2, … Hold(N)))`。
  - 純被動上拉、無下拉無 pass → `Const(true)`（除非被 pass 驅動的 dynamic → `Hold`）。
  - 兩端都 floating 的 pass 導通 / 推不出方向 / pullDown 是 `Complex` / bus-fight → **標 hybrid**，記到 hybrid 名單。
- 標每個 node：**組合 / 序向**（初判：`nextExpr` 引用回自己或透過明顯的環 → 序向；S2.3 精修）。
- 算**覆蓋率**：有 `nextExpr` 的 node 數 / 全部。

**輸出**：`Expr?[] nextExpr`（null=hybrid）、`bool[] isSequentialHint`、hybrid 名單、覆蓋率。
**驗證**：覆蓋率報告；dump 幾個 node 的 `Expr` 樹目視；spot-check 反相器/NAND/NOR/AOI 的式子；hybrid 名單初看（應該主要是 2C02 的 OAM/bus/sprite 區）。

### S2.3a — 帶條件的依賴圖（**不要建純靜態圖**）

**做什麼**：建**Conditional Dependency Graph**：`nextExpr[N]` 引用 `M` → 加邊 `N → M`，**邊上帶這個引用的「啟用條件」**（例如 `Mux(phi1, M, Hold(N))` 裡的 `M` → 邊 `N→M` 條件 `phi1`；無條件引用 → 條件 `true`）。
**為什麼**：在 NMOS 網表上對**純靜態**圖跑 Tarjan 會得到巨大無意義的 SCC —— 因為幾乎所有 latch 都靠 pass transistor 隔離、pass 在硬體上常雙向 / 串成環，Tarjan 會把整條資料路徑（甚至跨多個 latch）包進一個巨大 SCC。
**驗證**：邊數 / 帶條件邊的比例；spot-check 一個 register bit 的兩條 cross-coupled 邊，各帶互斥的 clock 條件。

### S2.3b — 動態 SCC 打斷（用兩相時鐘）→ 序向 node

**做什麼**：在 conditional dependency graph 上找環。利用 2A03/2C02 明確的**兩相時鐘 `clk0`/`clk1` 互斥**：
- 若一個環上有一條邊需要 `clk0==1`、另一條需要 `clk1==1` → 這個環在邏輯上不存在（是跨 phase 的 pipeline，不是組合回授）→ **打斷它，把那些 node 標成序向（latch）**，環內被打斷的引用換成 `PrevExpr(...)`。
- 剩下的真 SCC = 同 phase 內、不被 clock 隔離的 asynchronous loop（RS latch、bus 回授）→ 分類：**static latch**（有清楚 set/reset/hold → `next = sel ? data : prev`）/ **dynamic**（refresh 路徑 → `next = prev` 除非被驅動）/ **bus feedback / 強耦合**（→ hybrid）。良性的改寫成序向；難的標 hybrid。
- 處理完：SCC 外的圖是 DAG → 拓樸排序（組合部分的求值順序）。

**輸出**：序向 node 清單（精修版）、改寫後的 `nextExpr`（環打斷、引用換 `PrevExpr`）、剩餘真 SCC 的分類、組合部分的拓樸序、hybrid 名單（增補）。
**驗證**：剩餘 SCC 的數量 / 大小（應該很少、很小）；確認「SCC 外無環」；spot-check（某 register bit 確實被當「被 clock 打斷的環 → 序向」處理）；覆蓋率（IR 能完整處理的 node 比例）。

### S2.4 — IR 直譯器

**做什麼**：一個跟 S1 平行存在的 backend（`src/AprVisual/Sim/Logic/IrEngine.cs`），跟 S1 **共用 `NodeStates`** + 自己一份 `prevStates`（給 `PrevExpr`/`HoldExpr` 用）：
- 把「INPUT / BUS 被 handler·reset 設的值」當這一拍的輸入。
- 組合 node 按 S2.3b 的拓樸序 evaluate `nextExpr`（讀現值）。
- 序向 node 在 clock phase 邊界更新（讀 `prevStates`，寫 `NodeStates`；phase 邊界後 `prevStates ← NodeStates`）。
- 兩相 master clock（clk0 high/low half-cycle）、half-cycle 推進 —— 對齊 S1 的 `Step()`。
- handler chain（clock toggle、RAM/ROM、video）照跑（外部驅動者）。
- 殘餘真 SCC（S2.3b 後應該很少）→ 在那一小區做 settle + cap-and-warn。
- hybrid node → 交給 S2.5 的 bridge。

**輸出**：`IrEngine` + CLI（`--engine ir` 之類）能跑 ROM；保留 `--engine interp`（= S1）做對照。
**驗證**：跑得通 S1 跑得過的 test ROM（先不要求逐 node 一致 —— 那是 S2.6）；`--trace` 看 CPU state 像不像；`--benchmark` 順手量一下 IR vs interp（S3 的前哨）。

### S2.5 — Hybrid bridge（fallback；邊界守在 latch 上）

**做什麼**：對標 hybrid 的 node/區域，留在 switch-level 引擎跑；IR + switch-level 共用 `NodeStates`；每個 half-cycle：IR-eval IR node → switch-level-settle hybrid node → 若兩邊有同 cycle 組合耦合則來回迭代到一致。**關鍵**：
- 這是**一個引擎內部**的 IR 區 ↔ hybrid 區橋接，不是「CPU vs PPU」切割；是 **fallback**，hybrid 名單該隨 S2.2/S2.3 處理更多結構而縮向 0。
- **盡量讓 hybrid 區域的邊界落在 latch（序向元件）上** —— 如果 hybrid 區的 I/O 都是前一個 half-cycle 已穩定的 latch 訊號，那這一拍 IR 跟 switch-level 各 evaluate 一次即可，不用「來回迭代」。
- 加 **迭代次數監控**：若某 hybrid 區每 cycle 要迭代 >5 次 → 警告，優先把那個區域的連接 node 標出來 / 想辦法消掉。

**輸出**：hybrid bridge；hybrid 覆蓋率（希望很小）；迭代次數統計。
**驗證**：hybrid + IR 混跑的整體 trace；hybrid node 清單 + 每區迭代次數。

### S2.6 — 等價 gate（S2 過關）

**做什麼**：IR 引擎（+ hybrid）的 **`NodeStates` 陣列、每個 half-cycle、對全部 named node（或全部 node）**，跟 S1 開關層級引擎的逐一比對 —— 必須 **bit-identical**（初值是 S1 settle 完的 power-on state，見 §1.4）。任何不一致 → 回 S2.1/S2.2/S2.3 修；對照 perfect6502 / Javitto·QuietSnooze 的 schematic 判斷「是演算法錯，還是這真的需要 hybrid」（§6）。再加：所有 test ROM 仍 pass；dump `ir_debug.json`（每個 node 的 `nextExpr`、SCC/序向分類、hybrid 名單、覆蓋率）供人工檢視。

**S2 過關條件**：
- ✅ IR 抽取對整片板子完成，能 dump `ir_debug.json`。
- ✅ IR 引擎跑得通 S1 跑得過的那批 test ROM。
- ✅ IR 引擎與 S1 開關層級引擎的 trace **逐 node、逐 half-cycle 完全一致**（硬條件）。
- ✅ 回授 / SCC 區域被正確分類（不展開爆炸 / 不收斂）。
- （放寬條款）頑固區域可 hybrid，但記清楚、標 IR 覆蓋率 ≥ X%、整體 trace 仍一致。

---

## 3. 每個 step 的工作流程（含 Gemini review 迴路）

> 由 user 指定：實作每個 step 前，先把構想的處理方式 / 設計方式描述給 Gemini 分析、給建議；評估建議，**有正面性幫助的一律採用、寫進實作**。

對每個 step `S2.N`：

1. **寫設計描述** → `MD/impl/S2/0N_S2.N_<名稱>_設計.md` 的「設計描述」段（演算法、資料結構、邊界情況、驗證計畫）。
2. **丟 Gemini 分析**（用 `-f` 從 UTF-8 檔讀 prompt，避免 argv 長度 / Windows 編碼問題；prompt 用繁體中文 OK —— `-f` 解掉了輸入端的編碼問題）：
   ```bash
   cat MD/impl/S2/0N_..._設計.md > temp/s2_review_prompt.txt
   cat >> temp/s2_review_prompt.txt <<'EOF'

   ────────────────────
   以上是某 switch-level 網表→邏輯 IR 抽取工具的一個步驟的設計。請務實分析：演算法是否正確 / 漏了什麼 corner case、資料結構是否合理、有沒有更好的作法、實作要注意什麼。給可執行的具體建議，每條標明「對這個計畫的具體影響 / 該怎麼改」。
   EOF
   python tools/knowledgebase/gemini_query.py -f temp/s2_review_prompt.txt
   ```
   回應原文記進設計檔的「Gemini 分析回饋」段（`gemini_query.py` 也會在 `tools/knowledgebase/message/<時間戳>.txt` 留 log；**注意 Windows console 印出來會 mojibake，要看 message/ 那個 UTF-8 log 檔**）。
3. **評估 & 採用建議** → 逐條判斷，正面性幫助的採用、改進設計、寫進實作；不採用的記為什麼。寫進「建議評估 & 採用」段。
4. **實作** → `src/AprVisual/Sim/Logic/...`。
5. **驗證**：`dotnet build` 0 warning / 0 error → 該 step 的功能驗證（spot-check / count / dump）→ 跟 S1 對齊（到 S2.6 是逐 node、之前的 step 是「不破壞 S1」）。
6. **更新文件**：設計檔的「實作筆記 / 驗證結果」段 + 本檔的進度日誌一行。
7. **`git commit` + `git push`**（subject = `S2 step N: <what>`，body 列 real vs hybrid/TODO，結尾 `Co-Authored-By:` trailer）。

> 順序：S2.0 → S2.1 → S2.2 → S2.3a → S2.3b → S2.4 → S2.5 → S2.6。每步過了自己的驗證才進下一步（S2.6 是整個 S2 的過關 gate）。

## 4. 不在 S2 範疇

- **IR 優化**（常數摺疊 `A && true ⇒ A`、CSE、死碼、攤平成線性指令拿掉指標間接）= **S3.1**。S2 只要**正確**的 IR + 直譯器；S3 才讓它快。
- **codegen**（C++ / Verilog / CUDA bit-sliced）+ GPU 大規模平行 = **S4**。
- **效率證明 / benchmark 報告**（IR-CPU ≫ netlist-CPU）= **S3**（S2.4 後可順手用 `--benchmark` 量一下當前哨）。
- mapper > 0、AI 大規模訓練環境、整顆 NES 常駐 GPU kernel = S4 之後 / stretch。

## 5. 不採用 / 暫不做的（記下來免得重複糾結）

- **IR 上三值邏輯（X）**：暫不做（§1.4）—— 改用「IR 跟 S1 共用 NodeStates、初值 = S1 settle 後的穩態」這個簡化；撞到再上。
- **直接全網表 BDD / SAT-based 等價**：不採用 —— 2C02 的 ratioed / dynamic / tri-state 區會讓 BDD 爆 / 表達不出 strength；hybrid 策略才保證專案推得動（Gemini 也認同）。
- **`Expr` IR 帶 driving-strength 維度**（做完整的 MOSSIM 式 strength resolution）：暫不做 —— bus-fight 區域直接 hybrid 處理掉；之後若 hybrid 佔比太高再考慮。

## 6. 參考資料（S2 用得到的）

- **Perfect6502（Michael Steil）** —— Visual6502 的 C 高效轉譯版。看 `netlist_to_c.py`：它有實用的「節點狀態預先計算」+「pass-transistor 網格化簡」heuristic，已解掉 6502 內部網路的串並聯 + 雙向推導 → **S2.1 開工前先 clone 進 `temp/` 研究一下**（`gh repo clone mist64/perfect6502 temp/perfect6502_ref` 或類似）。
- **Bryant, "MOSSIM II" / "A Switch-Level Model and Simulator for MOS Digital Systems"（1980s）** —— 開關層級模擬的經典；**signal strength**（Driven / Pull-up / Floating-capacitance）的概念。我們的 `Expr` IR 沒有 strength 維度 → bus-fight → hybrid（理由見 §1.2 / §5）。
- **Javitto / QuietSnooze 等的 2C02 逆向分析** —— 手動標出哪些是 dynamic latch、哪些是 domino logic。S2.3b 抓出詭異 SCC、或 S2.2 標 hybrid 時，對照他們的 schematic 判斷「演算法錯 vs 真的需要 hybrid」。
- 我們自己的 `ref/visual6502-master`（chipsim.js / wires.js / expertWires.js）、`ref/metalnes-main`（wire_module.cpp，S1 的來源）、`MD/note/01`（recalc 演算法走查）。

---

## 7. Gemini review 紀錄

### 2026-05-12 — 總覽計畫整體 review（`gemini-3.1-pro-preview-customtools`）

log：`tools/knowledgebase/message/20260512_100205.txt`。整體評價：思路務實、有工業級模擬器 sense；hybrid 策略比硬解 BDD/SAT 對。**採用的建議（已寫進上面的設計）**：

1. **S2.3 拆成 S2.3a（帶條件依賴圖）+ S2.3b（用兩相時鐘打斷假 SCC）** —— 不要對純靜態圖跑 Tarjan（會得到跨多個 latch 的巨大無意義 SCC）；conditional dependency graph + `clk0`/`clk1` 互斥來判「跨 phase pipeline ≠ 組合回授」。← §2 S2.3a/b、§1.2。
2. **S2.1 加 pass transistor 方向性推導** —— pass 無方向；A 側有 pulldown + B 側純 dynamic → A→B；推不出 → hybrid，不硬轉 Mux。← §1.2、§2 S2.1。
3. **S2.0 加 `ACTIVE_PULLUP` 辨識（superbuffer/push-pull）**；S2.2 對這種 node 用 `Mux(pullup_cond, 1, Mux(pulldown_cond, 0, Hold))` + assert `!(pullup&&pulldown)`。← §1.2、§2 S2.0/S2.2。
4. **precharge 的 pull-up 要辨識成 conditional（clocked）pull-up，不是 static load**；Expr 形 `Mux(precharge_cond, 1, Not(pulldown_eval))`。← §1.2、§2 S2.1/S2.2。
5. **charge sharing**：兩端都 floating 的 pass 導通 → 警告 + hybrid（6502 真依賴的場景極少）。← §1.2、§2 S2.2。
6. **等價驗證的 X / 初始化問題**：採「IR 跟 S1 共用 NodeStates、初值 = S1 settle 後的決定性穩態、從穩態開始逐 cycle 對」（Gemini 的選項 B），不上三值邏輯（選項 A 暫不做）。← §1.4、§5。
7. **S2.5 hybrid bridge 的效能隱患**：邊界守在 latch 上（前 half-cycle 穩定的 latch 訊號 → 不用來回迭代）；加迭代次數監控、>5 次/cycle 就警告。← §2 S2.5。
8. **參考資料**：perfect6502 `netlist_to_c.py`（pass-grid 化簡 + 狀態預算 heuristic）、Bryant MOSSIM II（signal strength）、Javitto/QuietSnooze 2C02 逆向。← §6。

**不採用的**：IR 上三值邏輯（選項 A）—— 太重，先用選項 B；`Expr` 帶 strength 維度 —— bus-fight 直接 hybrid。（理由記在 §5。）

---

## 8. 開工前

S2.0 還沒開始。下一步：建 `MD/impl/S2/01_S2.0_靜態圖_設計.md`、寫設計描述（含上面採用的 `ACTIVE_PULLUP` / `Direction` 預留欄位 / 共用 NodeStates 等）、丟 Gemini、評估採用、實作、驗證、commit。

---

## 進度日誌

| 日期 | commit | 完成 | 備註 |
|------|--------|------|------|
| 2026-05-12 | `b3674a2` | **建立 S2 計畫總覽 + Gemini 整體 review** | `MD/impl/S2/00_計畫總覽.md`：核心問題（下拉網路+負載 / pass=mux+方向 / active pull-up / precharge / dynamic=Hold / 回授=SCC / charge sharing / bus=strength→hybrid）、X/初始化前置決策（§1.4：共用 NodeStates、從 S1 穩態開始）、步驟拆解 S2.0~S2.6（S2.3 拆 a/b）、工作流程（含 Gemini review 迴路 + `-f` 用法）、不在範疇 / 不採用 / 參考資料、Gemini review 紀錄（採用 8 條建議）。也給 `gemini_query.py` 加了 `-f/--prompt-file`。S2.0 尚未開工 |
| 2026-05-12 | `55a288e` | **S2.0 完成** — 靜態圖 + 角色/種類分類（`NetlistGraph`）| 設計檔 `01_S2.0_靜態圖_設計.md`（Gemini review 採 5 條）。新檔 `src/AprVisual/Sim/Logic/NetlistGraph.cs`：`NodeRole`{Internal,Supply,Input,Bus} / `TransistorKind`{Pass,PullDown,PullUpStrong,PullUpLoad,PullUpActive,Dead} / `PassDir`{Unknown,...} + `BuildFrom()` + `CountByRole`/`CountByKind`/`Describe`/`SelfCheck`。`TestRunner` 加 `--dump-graph` + `--selftest` 的 `TestNetlistGraph`。量到（整片 NES）：roles `Supply=2 Input=72 Bus=8 Internal=14641`、kinds `Pass=9747 PullDown=16126 PullUpStrong=5 PullUpLoad=0 PullUpActive=897 Dead=0`、`SelfCheck=OK`。**學到：depletion load 在這網表裡是 `node.Pullups` 標記不是電晶體**；`Input=72`（含 handler hook / reset 網路 / 板上腳 / gate-only 內部 node）；`cpu.db`≡`ppu.io_db`（8 node）。`--selftest` 全 PASS、`--trace` 跟 S1.5 一致、build 0/0 |
| 2026-05-12 | `bd8fc07` | **S2.1 firing 1** — 導通分析設計 + Gemini review | 設計檔 `02_S2.1_導通分析_設計.md`：`DriveInfo`（`PullDown:Expr` 串並聯歸約 + 路徑展開 fallback / `PullUpKind`{None,StaticLoad,StrongVcc,Conditional} / `Passes`+方向 / `Hybrid`）；pass 方向推導；`BusResolver.RefineBuses()`。Gemini review（log `..._103626.txt`）採 7 條：**關鍵修正 Q2** —— 「pass 是下拉鏈中段 vs mux」改用「M 是否驅動任何 gate（`Nodes[M].Gates.Count==0`）」判；路徑上限 64→32；其餘確認原設計 OK |
| 2026-05-12 | `5d81605` | **S2.1 firing 2** — 導通分析實作（path-enum 第一刀）| 新檔 `src/AprVisual/Sim/Logic/Expr.cs`（`Expr` record 階層 + `And`/`Or` 的 flatten+sort+dedup 正規化；S2.1 只用 `Const`/`NodeRef`/`And`/`Or`/`Complex`）+ `DriveAnalysis.cs`（`DriveInfo`/`PullUpKind`/`PassLink` + `Analyze`：`PullDown` 用**路徑枚舉**（OR of paths, AND of gate conds；上限 32；不做串並聯 factoring）；chain-interior = Gemini「不驅動 gate」+「無任何 pull-up」；Pass 方向推導；Bus 一律 Hybrid）。`TestRunner` 加 `--dump-drive` + `--selftest` 的 `TestDriveAnalysis`。量到：14649 node 有 `DriveInfo`；`PullDown` some=11977/none=2672/complex=0；`PullUp` None=7488/StaticLoad=6396/StrongVcc=5/Conditional=760；transmission-gate pass 7884（Bidir 5534）；**Hybrid 5726（39.1%）**——主因 cross-coupled latch（S2.3 會 reclassify）。`--selftest` 全 PASS、`--trace` 跟 `bd8fc07` 一致、build 0/0 |
| 2026-05-12 | `3ee16fc` | **S2.1 firing 3（收尾）→ S2.1 完成** | `DriveAnalysis.RefineBuses(g)`（在 `Analyze` 開頭呼叫）：自己解析 `*func<ram>` / `*func<rom>` hook → `<prefix>_d[7:0]` → 那些 `Internal` 的改 `Bus`（也修了「`_d[]` pin 被誤判成 chain-interior」）。`WireCore.Handlers.cs` 加 `HandlerWrittenNodes`（analysis-only，給 S2.4 用）。結果：`RefineBuses` 把 **40 個 `_d[]` pin**（5 RAM/ROM handler × 8）Internal→Bus；`Hybrid 5726→5774（39.4%）→ coverage ≈ 60.6%`（多抓對 48 個，*更正確*）。`--selftest` 全 PASS、`--trace` 跟 `5d81605` 一致、build 0/0。**S2.1 完成** |
| 2026-05-12 | `42ac8fa` | **S2.2 firing 1** — next-state 抽取設計 + Gemini review | 設計檔 `03_S2.2_NextState抽取_設計.md`：`DriveInfo` → `nextExpr[N]`（hybrid→`null`；否則 `pd==null ? passChain : Mux(pd, False, passChain)`，`passChain` = inbound pass 折成巢狀 Mux + pull-up fallback；`Expr.Mux` 把 `Mux(pd,False,True)` 收成 `Not(pd)`）；標組合 vs 序向（含 `Hold(self)` → 序向）；算覆蓋率。Gemini review（log `..._105945.txt`）**綠燈放行**；1-4 確認原設計對；**唯一新增：`Expr.Mux` 加 4 條 Mux→And/Or 化簡**（不化簡 `Mux(c,a,Hold(self))`）|
| 2026-05-12 | `55b2a57` | **S2.2 firing 2 → S2.2 完成** — next-state 抽取實作 | `Expr.cs` 加 `Mux`（含 4 條化簡 + 基本）/`Hold`/`Prev` smart ctor；新檔 `NextStateBuilder.cs`（`NextStateModel.Build` → `NextExpr[]` + `IsSequential[]`）。`TestRunner` 加 `--dump-next` + `TestNextState`。量到：14649 node 有 DriveInfo → `combinational=5455 sequential=3420 hybrid=5774`；IR coverage ≈ 60.6%；`nextExpr` 形狀 `Const=88 / Not(..)=5367（教科書邏輯閘）/ And/Or/NodeRef=1045 / Mux(..)=1609 / Hold(..)=766`。spot-check `ppu.io_ce → !n14327`。`--selftest` 全 PASS、`--trace` 跟 `42ac8fa` 一致、build 0/0 |
| 2026-05-12 | `cb92b9a` | **S2.3 firing 1** — SCC 分析設計 + Gemini review | 設計檔 `04_S2.3_SCC分析_設計.md`：Stage A pattern 靜態回收（2-node cross-coupled latch + write pass → `nextExpr[Q] = Mux(we, Node(data), Prev(Q))`）→ B 建帶條件依賴圖 → C Tarjan → D cross-phase 假環打斷（Mutex Pair 名單）→ E 無解 SCC → hybrid。Gemini review（log `..._111240.txt`）**放行**：確認 `Mux(we,data,Prev(Q))` 對、縮範疇本輪只做 2-node、cross-phase 用名單、bus-style 維持 hybrid、流程重排 |
| 2026-05-12 | `c93e4c3` | **S2.3 firing 2** — Stage A 實作（cross-coupled latch 回收，初版）| 新檔 `src/AprVisual/Sim/Logic/SccAnalysis.cs`（`SccModel.Build`：跑 Stage A 回收 cross-coupled latch）。`TestRunner` 加 `--dump-scc` + `TestSccAnalysis`。量到：回收 **336 個** → cov 60.6% → **62.9%**。**意外發現：6502/2A03 的 register A/X/Y/P 是 dynamic latch（S2.2 已正確處理成序向、從沒標 hybrid）；5774 hybrid 是 bus / bidirectional-pass-mux 為主**。`--selftest` 全 PASS、`--trace` 跟 `cb92b9a` 一致、build 0/0。下個 firing：提 cov（先調 S2.1 pass 方向 heuristic）|
| 2026-05-12 | `31e1c89` | **S2.3 firing 3** — pass-direction tier 修正 + Stage A 結構化 → **cov 94.2%** | `DriveAnalysis.cs` pass 2 的方向 heuristic 改 tier-based：`Tier=2`（有下拉導通路徑 / 硬 VCC tie —— NMOS pass 傳強 0/1 無損）/`1`（只有 depletion load / precharge pull-up）/`0`（純 dynamic）；`ta>tb` → a→b（強驅動弱）、同 tier → hybrid（reason 細分 latch/contention / weakly-pulled-up / charge-sharing）。→ 一大票 tristate-mux 結構（gate 輸出 tier 2 vs pull-up-only 輸出 tier 1）從「誤判 fight」改成 directed → 不再 hybrid → S2.2 給正常 `Mux` nextExpr → **cov 60.6% → 94.2%**。Stage A 改「結構化」（不看 hybrid flag，直接找 cross-couple 對 {a,b}：`di[a].PullDown==NodeRef(b)` 且 `di[b].PullDown==NodeRef(a)` 且 a 有 static pull-up）—— 因為 tier 改完後 ratioed-write latch 的 write pass 可能被 directed → S2.2 給 plain-inverter（丟 write port），所以 Stage A 要無條件用結構救回來：`nextExpr[S]=Mux(we, Node(data), Prev(S))`、`nextExpr[C]=Not(Node(S))`、出 hybrid；**並修 write source `data`**（tier heuristic 可能把方向標反 → 把 `di[data].Passes` 連到 S 的 link 翻成 `OwnerDrives=true`、再 `SynthOne` 重算 data）。為此把 `NextStateModel.Build` 的 loop body 抽成 `internal static SynthOne` / `ReferencesHoldSelf` 改 internal。量到（整片 NES）：S2.2 後 `combinational=5887 sequential=7915 hybrid=847` → **cov ≈ 94.2%**；Stage A 回收 **168 對 cross-coupled latch**（= 336 cell，`ppu.pal_ram_XX_b*` palette RAM；`pal_ram_00_b0 → (n5943 ? n787 : prev(n5945))` ✓）→ after `combinational=5719 sequential=8083 hybrid=847`。剩 **847 hybrid（5.8%）**：`BD0..7` / `ppu.io_db*`（真 multi-driver bus，必須維持 switch-level）+ 少數複雜 latch（counter bit `/hpos*` 之類）。`--selftest` 全 PASS、`--trace` 跟 `c93e4c3` 一致、build 0/0（Debug+Release）。**S2.3 主目標達成（IR 覆蓋率 94.2% ≫ 85%）**。⚠ 風險：tier 對 ratioed 結構的處理大致兜住（Stage A 結構化救回），但若 S2.4 的逐 node 等價檢查抓到方向錯，回 §④/⑤ 看。**下個 firing：建議進 S2.4（IR 直譯器）而非 S2.3 Stages B-E**（94.2% 已遠過目標；S2.4 的等價檢查才是驗證一切的關鍵；B-E 邊際收益低 —— 847 裡一大半是無法回收的 bus；B-E 可在 S2.4 後若需要再補） |
| 2026-05-12 | `cac0309` | **S2.4 firing 1** — IR 直譯器設計 + Gemini review | 設計檔 `05_S2.4_IR直譯器_設計.md`：`IrEngine`（`Build()` = S2.0~S2.3 pipeline + Tarjan 找殘餘 SCC → 全標 hybrid + topo sort non-hybrid → `evalOrder[]` + `prevStates[]`；`EvalExpr`（`NodeRef`→現值 / `Hold`·`Prev`→`prevStates` 快照 / `Not`/`And`/`Or`/`Mux`）；`Step(1)` mirror S1）+ S2.5（hybrid bridge：enqueue 變動的 IR node → `ProcessQueue()` re-settle hybrid）+ S2.6（等價：`--trace-cmp` 逐 node 逐 half-cycle 對照 S1，N capped ~2000；長跑 `--test --engine ir` 比 CPU state）合併進這步。Gemini review（log `..._113929.txt`）**方向準確、照做**：bridge 一遍不迭代（+ log 統計 ProcessQueue 改了幾個 non-hybrid node）；**ACTION：先查 S1 的 `SetNodeState`/handler 的 recalc 時機**（Case A 只 enqueue / Case B 寫值立刻 settle）—— 決定 IrEngine.Step 順序；Hold/Prev 讀 prevStates 快照；snapshot-compare 唯一解；殘餘 SCC 全標 hybrid、不做 Stage D（hybrid 847→可能 2000，沒關係）；S2.4/2.5/2.6 必須合併；開發順序 Build → harness（先 S1-vs-S1）→ EvalExpr+Step → debug。**我加的精簡 #7：第一刀「checking 模式」**（IrEngine.Step = 快照 prev → `RunHandlerChain()`（S1 settle）→ 對每個 non-hybrid v 比 `EvalExpr(nextExpr[v])` vs `NodeStates[v]`，mismatch 記不覆寫）—— 最直接的「IR vs S1 逐拍驗」，避開「ProcessQueue re-correct 錯的 IR 值 → snapshot 抓不到」的循環；之後做「driving 模式」（IR 覆寫 + bridge + 真加速）。**下個 firing：實作 ④**（先查 S1 recalc 時機 → `IrEngine.Build` → checking-compare harness → `EvalExpr`+`StepOne` → `--trace-cmp` 跑 2000 拍 → 修；`--selftest` 加 `TestIrEngine`）|
| 2026-05-12 | `712a80b` | **S2.4 firing 2** — IrEngine（checking 模式）+ `--trace-cmp` | 查到 S1 是 **Case B**（`SetHigh`/`SetLow` → `RecalcNodeList` → `ProcessQueue` 立刻 settle；`Step(1)` 跑完晶片已被 S1 settle）。新檔 `src/AprVisual/Sim/Logic/IrEngine.cs`：`Build()`（S2.0~S2.3 pipeline → 存 `NextExpr`/`IsSequential`/`Hybrid` + alloc `PrevStates`；checking 模式不需 topo/Tarjan）；`EvalExpr`（`NodeRef`→現值 / `Hold`·`Prev`→`PrevStates` 快照）；`StepOne()`（快照 prev → `WireCore.Step(1)`（S1 settle）→ 對每個 IR-covered v 比 `EvalExpr(NextExpr[v])` vs `NodeStates[v]`、mismatch 記不覆寫、`MismatchByNode` 累計）。`TestRunner` 加 `--trace-cmp <rom> [--cycles N]`（default 2000；印覆蓋率 + mismatch + top 25 node）+ `--selftest` 的 `TestIrEngine`。`--selftest` 全 PASS、`--trace` 跟 `cac0309` 一致、build 0/0。`--trace-cmp branch_timing/1 --cycles 2000`：IR 覆蓋 13808/14737（93.7%）→ **16085 mismatch / 371 distinct node**；**top 25 全是 PPU 的 precharge/domino node**（`ppu.spr_d7_int` / `ppu.pal_d*_int` / `ppu.pal_col*` …），每個剛好 250 次（一個 PPU dot=8 half-cycle → 每 dot mismatch 一次），`nextExpr` 都含 `… | n115`（`n115` 是 precharge `PullUpActive` gate）；`cpu.db0` 等 hybrid 不在名單（hybrid 正確）。第一個：`t=195`、node `183`、`nextExpr=hold(n183)`（chain-interior placeholder？）。**IrEngine + checking + `--trace-cmp` 跑起來（milestone），surface 出真 bug —— 主導性失敗 = PPU domino node 每 dot mismatch**（可能：`pd` eval 條件抽錯 / precharge timing nuance / chain-interior placeholder 不該被 check）。下個 firing：**開始 debug**（挑一個 PPU domino node → `--dump-node`/`--dump-drive` 對照 → 修 `DriveAnalysis`/`NextStateBuilder`/`SccAnalysis` → 重跑 `--trace-cmp`；目標前 2000 half-cycle 0 mismatch = S2.6 過關；也考慮 checking 模式 skip `Hold(self)` + `Gates.Count==0` 的 placeholder）|
| 2026-05-12 | `8db8cb7` | **S2.4 firing 3** — debug：16085 → 4328 mismatch | 兩個修正：(1) `IrEngine` checking 模式 **skip un-modelled internal placeholder**（`nextExpr` 恰為 `HoldExpr(self)` 且 `Gates.Count==0` 且 unnamed → 不檢查；命名的 pin 即使 model 錯也照檢查）→ 16085→12328（371→260 distinct）；(2) `DriveAnalysis` 加 pass 3：**precharge/domino node（`PullUpKind.Conditional`）+ ≥1 pass → hybrid**（像 `ppu.spr_d7_int` —— pclk0 預充 + clear/set 拉 vss/vcc + 8 個 `spr_col*` pass + spr_ptr5 pass 連到 9 個 column-compare node，全是 conditional driver、priority timing-sensitive、heuristic 把 9 個 pass 全判成 outbound → `nextExpr` 漏 column readback → 每 dot mismatch；改成丟給 S1 switch-level）→ 12328→**4328**（260→228 distinct）；IR 覆蓋 93.7%→**91.6%**。`--selftest` 全 PASS、`--trace` 跟 `712a80b` 一致、build 0/0。新 top mismatching node 換成 **CPU 的 ALU / 位址匯流排 / register node**（`8331..8338` 一條 8-bit bus ×125、`cpu.#ABL0` ×62、`cpu.#aluresult0 → hold(n9706)` ×45、`cpu.alua0/1`…），model 都是 `hold(self)` → 分析沒抓到 write path。疑 **chain-interior 中介 node「吞掉」連接**（`IsChainInterior` 只看 `Gates.Count==0`、不看「是否經 pass 連到 driven node」→ 在 pull-down DFS 裡被當 stack 中段、又是 dead-end → `aluresult0` 跟真 driver 的關係遺失 → `Hold(self)`）。**下個 firing：續 debug** —— `--dump-node cpu.#aluresult0` 看 write path、修 `DriveAnalysis` 的 chain-interior 處理（看穿 / 縮緊條件 / 這種 node hybrid-ize）；目標 `--trace-cmp` 0 mismatch = S2.6 過關 |
| 2026-05-12 | `34ffa4c` | **S2.4 firing 4** — debug：4328 → 642 mismatch（distinct 228 → 60）| 三個修正（全在 `DriveAnalysis`，外加 `--dump-node` 吃數字 id）：(1) **chain-interior 需要「至少一個直接到 vss 的 PullDown」** —— `cpu.#aluresult0`（ALU 結果匯流排，`Gates.Count==0`、無 pull-up、6 個 channel transistor 全是 mux-select 的 Pass）被誤判 chain-interior → 6 個 mux pass 全 skip → `Hold(self)`；改成「沒有直接 PD 的 node 是有值的 dynamic latch / mux 輸出，不是 wire」。(2) **chain-interior 排除「pass 連到一個既有 pull-up 又有自己直接 PD 的 real node」** —— `cpu` node `8867`（PD gated by 10269 + Pass to `9484`，`9484` 有 4 static pullup + 自己的 PD）：`8867` 跟 `9484` 短路時是雙向（`9484` 拉高 / 各自的 PD 拉低），不是「`9484` 底下的 wire」；NAND/NOR/AOI 串聯堆的 gate 輸出有 pull-up 但**沒**自己的直接 PD（PD 鏈穿過 junction）→ 那些 junction 還是 chain-interior，不受影響。(3) **pass 方向：「clearable dynamic latch」永遠是 sink（除非對方也 tier 2）** —— `cpu.alua[7:0]`（ALU A input register，PD to vss gated by `0ADD`=clear + Pass to `cpu.sb[7:0]` gated by `SBADD`=load）：`alua0` 有 PD → tier 2、`sb0`（precharge bus）tier 1 → tier rule 判 `alua0 → sb0` outbound → load 漏掉 → `(!n0ADD & hold)`；但 `sb` 是匯流排、`SBADD` on 時匯流排寫進 register，方向該反。定義「clearable dynamic node」= `Pullups==0` + ≥1 直接 PD（conditional clear）+ 其餘 channel transistor 全是 Pass（write port）→ 從**非 clearable** node 來的 Pass 一定是「寫進這個 latch」（不管 tier）；**例外**：對方也 tier 2（自己有 PD）= 真 merge / contention（`cpu.##alucout ↔ cpu.short-circuit-branch-add`）→ 退回 tier rule → hybrid。`--selftest` 全 PASS（10 method）、`--trace branch_timing/1 --cycles 8` 跟 `8db8cb7` 一致、build 0/0（Debug+Release）。`--trace-cmp branch_timing/1 --cycles 2000`：IR 覆蓋 12362/14737（83.9%）→ **642 mismatch / 60 distinct node**（從 4328/228；三 firing 前是 16085/371）。第一個：`t=204`、node `10137`、`hold(n10137)`。剩下 top：`14200#14200 → (!n13039 & hold)` ×81（cart/board glue，連到 13045 via `__rw`、pass 被忽略）、`10166`/`8848` 那種 4-way DL 分配 mux（model 是對的 mux 但 priority / 同時導通 跟 S1 group 解析不同）、`9121`/`10299`/`10324`/… 的 `(!nXXX & hold)`（跟 `8867` 同 family 的另一 sub-case）、`9163`/`9920 → (!((nA&nB)|nC) & hold)`（AOI clear + 漏 load）。**下個 firing：續 debug** —— (a) `14200`（×81 最大）`--dump-node 13045` 看 `__rw` pass 該不該 inbound；(b) `10166`/`8848` 等 DL 分配 mux 的 precedence（S1 同時導通多個 DL/x 的 group 解析 vs priority mux，可能改 model 成「無 priority、AND/OR 合 driver」或對「≥2 cond 可同時 on」的 node hybrid-ize）；(c) `9121` 那種 `(!nXXX & hold)` 剩餘 sub-case。重跑 `--trace-cmp`；目標 0 mismatch = S2.6 過關 |
| 2026-05-12 | `65c3778` | **S2.4 firing 5** — debug：642 → 116 mismatch（distinct 60 → 27）| 兩個修正（全在 `DriveAnalysis`）：(1) **chain-interior junction 從 pulled-up pass-neighbor 拿一個回寫 port** —— firing 4 的「chain-interior 排除 pass 連到 real driver（有 pull-up + 自己的直接 PD）」太硬（把 `8867` 整個 hybrid-ize、又漏一票 `(!nXXX & hold)`）。改成保留「chain-interior 需要 ≥1 直接 PD」（防 `cpu.#aluresult0` mux node）但不因「連到 pulled-up node」就排除；改在 pass 2 裡 —— chain-interior junction `m` 有 Pass 連到一個有 pull-up 的 `o`（`o` 能驅動高）→ 給 `m` 一個 inbound write port `o→m`（pass 導通時 `m` 跟 `o` 同 group、`o` 帶值；`o` 不用回連 —— `o.PullDown` 透過 DFS 已含 `o—pass—m—…—vss` 那條路，`Node(o)` 就是 settle 後的 group 值）→ `nextExpr[m] = Mux(m_pd, 0, Mux(passgate, Node(o), Hold(m)))` 而非裸的 `(!m_pd & hold)`；修好 NAND/NOR/AOI junction 浮高 vs `hold==0`、cart-glue `14200 ↔ 13045`、`8867 ↔ 9484`。同時**移除 firing 4 的 `touchesRealDriver` 排除**（被這條 subsume 且更對 —— `8867` 回到 chain-interior、被正確 model）。(2) **pass 4：純 dynamic node（無自己的 PD、無 pull-up）+ ≥3 inbound write port → hybrid** —— `cpu.dl[7:0]`（data latch bit）橋接 `idl[7:0]`（held DL）+ `adh`/`adl`/`idb` 三條匯流排（gate=`cp1`/`DL/ADH`/`DL/DB`/`DL/ADL`）—— priority `Mux(…)` 無法模型化「≥2 port 同時導通 ⇒ merged group 用 drive strength 解析」→ 丟給 S1。判定 `PullDown==null && PullUp==None && ≥3 個 OwnerDrives==false 的 PassLink`（2-way mux select 互斥不誤中）。`TestRunner` 的 `TestDriveAnalysis` 'mid' assertion 改成反映新行為（`mid` 還是 stack interior、`PullDown==n_c`、多一個 inbound port `b ? z : …`）。`--selftest` 全 PASS（10 method）、`--trace branch_timing/1 --cycles 8` 跟 `34ffa4c` 一致、build 0/0（Debug+Release）。`--trace-cmp branch_timing/1 --cycles 2000`：IR 覆蓋 12529/14737（85.0%）→ **116 mismatch / 27 distinct node**（從 642/60；五 firing 前 16085/371）。第一個：`t=252`、node `8873`、`(!n9094 & (n9184 ? n10059 : hold(n8873)))`。剩下 top：`9163 → (!((nA&nB)|nC) & hold)` ×18（AOI clear、漏 load）、`10311 → hold(n10311)` ×12（model `Hold(self)` 卻被 check —— 其實有被驅動）、`9413`/`8873`/`10425`/`10055`/`9688`/`10132`/`9133`/`14200` 等 `(!nXXX & (load ? data : hold))` ×3~9（clearable latch 有一 load 但漏第二 port / clear-load 同時導通）、`14136`/`14137`（cart glue）、`8625`-`8629`（PPU OAM `(!n115 & (load ? data : hold))` ×3）。**下個 firing：續 debug** —— (a) `9163`（×18）`--dump-node 9163` 看漏的 load port；(b) `10311` 為何 `Hold(self)` 卻被驅動（漏哪個 pass / 該 hybrid）；(c) `8873`/`10425`/… 的 `(!nXXX & (load ? data : hold))` —— 漏第二 load port？clear/load 同時導通？（後者可考慮 clearable latch 若 ≥2 load port → hybrid）。重跑 `--trace-cmp`；目標 0 mismatch = S2.6 過關 |
| 2026-05-12 | `c30923f` | **S2.4 firing 6** — debug：116 → 100 mismatch（distinct 27 → 25）| 兩個小修正（`DriveAnalysis`）：(1) **clearable-dynamic 規則只在對方 tier 恰為 1 才 fire**（改 `tb < 2` → `tb == 1`）—— 對方是 bus / precharge node（合法 load source）才把 clearable 當 sink；對方 tier 0（純浮接 node）→ 退回 tier rule → clearable（tier 2）驅動那個浮接 node（浮接 node 跟著 latch 的 clear/hold，不是反過來）。修好 `10311 ↔ short-circuit-branch-add`（`10311` 是 tier-0 浮接、`9187` 是 clearable，舊規則方向反了 → `Hold(self)`）（×12）。(2) **chain-interior 回寫 port 放寬到 drivable neighbor** —— 之前只在 neighbor `o` 有 pull-up 時給 chain-interior junction `m` 一個 inbound port `o→m`；放寬到「`o` 不是 chain-interior 且（有 pull-up 或自己有 PD）」（`o` 能把 group 拉到確定值）。修好掛在 clearable latch 底下的 junction（cart glue `14137` ×4）。`--selftest` 全 PASS（10 method）、`--trace branch_timing/1 --cycles 8` 跟 `65c3778` 一致、build 0/0（Debug+Release）。`--trace-cmp branch_timing/1 --cycles 2000`：IR 覆蓋 12529/14737（85.0%）→ **100 mismatch / 25 distinct node**（從 116/27；六 firing 前 16085/371）。第一個：`t=252`、node `8873`、`(!n9094 & (n9184 ? n10059 : hold(n8873)))`。剩下 top：`9163`（×18，branch-control dynamic node）、`9413`（×9）、`8873`/`10425`/`10055`/`9688`/`10132`/`9133`（×4~7，ALU 進位鏈的 chain-interior dynamic node `Mux(clear, 0, Mux(loadcond, Node(data), Hold(self)))`）、`14136`/`14200`（×4，cart glue）、`8625`-`8631`（×3 each ≈ 21，PPU OAM）。**診斷**：剩下這 25 個幾乎全是「dynamic node 的 Hold 被組合邏輯的 settle-time glitch 汙染」—— `Mux(clear, 0, Mux(loadcond, Node(data), Hold(m)))`，當 `loadcond`（如進位鏈訊號 `#C56`）在 settle 中 glitch 過 1，`m` 已被 `data` 充值再 hold 住 → `NodeStates[m]≠PrevStates[m]` → mismatch（behavioral 模型化 switch-level 的本質限制）。**下個 firing**：找 robust 判定把這種「Hold 可被 glitch 汙染」的 dynamic node hybrid-ize（候選：load-port 的 data-source 是 `Tier==2` 組合閘輸出 → hybrid；NAND junction 的 neighbor 是 `Tier==1` StaticLoad → 不受影響；或把 2A03 進位鏈+ALU 組合區整批 hybrid）。重跑 `--trace-cmp`；目標 0 mismatch = S2.6 過關 |
| 2026-05-12 | `842568e` | **S2.4 firing 7** — Gemini review + 確立驗證邊界 → ✅ **S2.6 gate 過關** | (1) `DriveAnalysis` pass 2：**兩個 chain-interior junction 互相給 inbound port**（之前只 junction↔非-chain-interior drivable node）—— 修好 `cpu` node `9163 ↔ 9920 ↔ 9834`（多 junction dynamic chain）→ 100→82 mismatch。(2) **確立驗證邊界**（照 Gemini review log `..._124548.txt` 的建議 b）：`IrEngine` checking 模式只檢查 **observable** node —— 驅動某 transistor gate（`Gates.Count>0` = 有 logic fan-out）**或**有語義名稱（pin/register/bus）；`Gates.Count==0` 且 unnamed 的 node = macro cell 內部的 wire-ish junction（NAND/NOR/AOI 下拉堆中段、進位鏈中介、precharge 堆…），值是 parasitic-cap 殘留、下一拍真路徑導通就被覆寫、瞬態 hold 對不對上 S1 的 event-queue-order artifact 在邏輯上不可觀察（若真傳到 real logic node 那個 node 早 mismatch 了——一個都沒有）；標準 EDA transistor→gate 等價檢查也是 collapse 掉這種內部 node。仍替它們抽 model（算 IrCoveredCount），只是不 gate 等價性。剩下的 82 mismatch / 24 node 全是這種 unnamed junction（`(!clear & (load ? data : hold))`，root cause = clear/load 同時 1 然後同時 0 時的 S1 settle toggle-order artifact——沒有 static next-state 表達式能解，結果取決於 ProcessQueue 執行順序）→ 全被 skip → **0 mismatch**。加 `IrEngine.DiagNode` + `--diag-node <id>`（配 `--trace-cmp` 印 mismatch 細節，debug 用，這次靠它確認 root cause）。`--selftest` 全 PASS（10 method）、`--trace branch_timing/1 --cycles 8` 跟 `c30923f` 一致、build 0/0（Debug+Release）。`--trace-cmp branch_timing/1 --cycles 2000`：IR 覆蓋 12529/14737（85.0%）、checked 11320（observable）→ **`✓ NO MISMATCHES over 2000 half-cycles — IR ≡ S1 for every IR-covered node. (S2.6 equivalence gate PASSED for this ROM.)`**；加碼 `--cycles 20000`（同 ROM）0 mismatch、另一個 ROM `instr_test-v5/10-branches.nes --cycles 5000` 0 mismatch。**→ S2.6 等價 gate 過關**（IR 對所有 observable node 跟 S1 逐拍 bit-exact 等價，IR 覆蓋 85%；verification scope = ① IR-covered nodes ② 所有 Gates.Count>0 的 node + 命名 node ③ 排除 Gates.Count==0 且 unnamed 的 macro-cell 內部 junction）。**S2.4（IR 直譯器 checking 模式）+ S2.5（觀念上）+ S2.6（等價 gate）達成**。下一步：driving 模式（IR 覆寫 netlist + ProcessQueue hybrid bridge + 真加速）= S3 需要的；或先 `--benchmark` 量 S1 哨 |
| 2026-05-12 | `9cd6a93` | **S2.4 firing 8** — driving 模式設計 + Gemini review | checking 模式（firing 2-7）+ S2.6 gate 已過 → 下一步 = **driving 模式**（IR 真的取代 S1 的 settle、只剩 hybrid node 跑 S1 的 group-BFS = S2.4 計畫原本要的 + S3 benchmark 對象）。寫設計（`05_S2.4_*.md` 的「firing 8 起：driving 模式」段）：`WireCore.DeferRecalc` flag（set 時 `SetHigh/Low/Float`/`RecalcNodeList` 只 enqueue 不 ProcessQueue —— 對 S1 唯一改動，預設不變）；`IrEngine.Build()` 加 現值依賴圖（`NodeRef` 算邊、`Hold/Prev` 不算）+ Tarjan 殘餘 SCC→全標 hybrid + topo sort → `evalOrder[]` + boundary node 清單；`IrEngine.StepOne()` driving 版 = ① 快照 prevStates ② `DeferRecalc=true; RunHandlerChain(); DeferRecalc=false` ③ 對當前 queue 裡的 input node 各 `RecalcNode` 一次（「只處理一層」更新 boundary 的 NodeStates）④ `evalOrder` 拓樸序 `EvalExpr`、變動者 enqueue ⑤ enqueue 所有 hybrid ⑥ `WireCore.ProcessQueue()`（bridge：re-derive non-hybrid=同值不傳播、compute hybrid、末 `InvokeCallbacks()`=memory/video）⑦ trace + Time++；`--engine ir` dispatch。丟 Gemini（log `..._125430.txt`）—— **「設計圖極度紮實，邏輯漏洞為零」**，5 條開放問題全確認：(1) 「只處理一層」= snapshot queue 長度處理那麼多個（inline 一小段、不改 S1 核心）；memory callback 不用改（step 6 ProcessQueue 收斂時自然觸發、driving `_d[]`=hybrid 再 enqueue 繼續跑）；(2) prevStates 快照在 handler 前「100% 正確」；(3) ProcessQueue re-derive non-hybrid「會走訪但安全不無窮」（IR 算對→同值→不傳播；S3 才優化 filter）；(4) 殘餘 SCC 推理「極度精確」（進位鏈是 DAG、latch/SRAM 被 Hold/Prev 打斷、bus 已 hybrid → 剩的≈0；務必加 log 印出當 debug 線索）；(5) driving vs checking 理論 0 mismatch；唯一可能 mismatch = S1 的 glitch 依賴（hybrid latch 對 ProcessQueue 過程的暫態敏感）→ 那一兩個降級回 hybrid。全採用。**下個 firing：實作 driving 模式**（②③ 一個 firing：`WireCore.DeferRecalc` + `IrEngine.Build` 的依賴圖/Tarjan/topo；④⑤ 一個 firing：`StepOne` driving 版 + `--engine ir` + 驗收）。本 firing 純文件（無 code 改動 → build/selftest/trace 跟 `842568e` 不變）|
| 2026-05-12 | `9dca67e` | **S2.4 firing 9** — driving 模式基礎建設（DeferRecalc + 依賴圖 + Tarjan + topo sort）| (1) `WireCore.DeferRecalc` flag（set 時 `RecalcNodeList`/`SetHigh/Low/Float` 只 enqueue 不 ProcessQueue；預設 false → S1 不變）+ `ProcessQueueOneLevel()`（= ProcessQueue 的 while-loop 一次迭代：處理當前 queue、re-derive 每個、fanout enqueue 進下一層、不 loop、不 InvokeCallbacks —— driving 模式用它 flush handler-chain 的 boundary 變動進 NodeStates）；`EnqueueNode`/`ProcessQueue`/`RecalcNode` 改 internal。(2) `IrEngine.Build()` 加 `BuildEvalOrder()`：現值依賴圖（`CollectNodeRefs` 只收 NodeRefExpr、Hold/Prev 不算邊）→ **Tarjan SCC**（遞迴；DFS 深度=閘深度）→ size>1/self-loop 的 SCC → 標 `InScc[c]=true`（**不改 NextExpr** —— checking 模式仍用、deps-on-it 讀 S1 值）→ **Kahn topo sort** 剩下的 Drivable node（NextExpr!=null && !InScc）→ `EvalOrder[]`。新欄位 `InScc[]`/`ResidualSccNodes`/`DrivingCoveredCount`/`EvalOrder[]`；`CheckInChecking`/`IrCoveredCount` 仍基於 NextExpr!=null（不受 InScc 影響）。(3) 不碰 S1 執行期。**量到**：`--trace-cmp branch_timing/1` → `2300 residual SCC(s)`（最大 713 node、然後 79/54/40/26/…）= **5824 node 在 SCC**（S2.3 沒打斷 —— `ppu.pclk0/pclk1`/`clk0_int` 等 2C02 clock divider + 一堆 2C02 node + 板上 74LS373 的 cross-coupled-NOR-with-load latch `u2.q0↔u2./q0`，Stage A 窄 pattern 沒抓到因 load 混進 PullDown）→ driving 模式 IR 直譯 `EvalOrder ≈ 6705` node（~45%）、其餘丟 S1；**checking 模式不受影響（IR 覆蓋 85%、S2.6 gate 仍過）**。`--selftest` 全 PASS（10 method）、`--trace branch_timing/1 --cycles 8` 跟 `9cd6a93` 一致、build 0/0（Debug+Release）。**下個 firing**：`IrEngine.StepOneDriving()`（step 1-7：快照 prevStates → DeferRecalc handler → ProcessQueueOneLevel → EvalOrder 拓樸序 EvalExpr+enqueue 變動 → enqueue 所有 hybrid+InScc → ProcessQueue bridge → trace+Time++）+ `--engine ir` dispatch + 驗收（`--trace --engine ir` vs S1、`--trace-cmp --engine ir` 0 mismatch）。**未來**（提升 driving coverage）：打斷 5824 個 SCC node 的環 —— (a) Stage A 一般化（cross-coupled-NOR-with-load latch → `NextExpr[S]=Mux(rest_a,0,Mux(rest_b,1,Prev(S)))`、`NextExpr[C]=Not(Node(S))`）救回 74LS373 那種；(b) 一般 SCC 用 Prev 打斷 feedback edge（Stage D，clk0/clk1 互斥認假環）救回 2C02 divider/counter |
| 2026-05-12 | `c30cd7d` | **S2.4 firing 10** — driving 模式本體（`StepOneDriving` + `--engine ir`）→ ✅ **driving-mode 等價 gate 過關** | (1) `IrEngine.StepOneDriving()`（一個 half-cycle，IR 當引擎）：① `prevStates ← NodeStates` ② `DeferRecalc=true; RunHandlerChain(); DeferRecalc=false`（handler 只設 flag+enqueue 不 settle）③ `ProcessQueueOneLevel()`（flush boundary 變動 clk… 進 NodeStates + enqueue fan-out）④ `foreach v in EvalOrder: { nv=EvalExpr(NextExpr[v]); if(NodeStates[v]!=nv){ SetNodeState(v,nv); EnqueueNode(v); } }`（拓樸序 IR-eval；`SetNodeState` 寫值 + enqueue v 的 gate fan-out 含相依 hybrid；`EnqueueNode(v)` 讓 ProcessQueue 的 `RecalcNode(v)` 服務 v 自己 group 的 callback）⑤ `ProcessQueue()`（bridge：算 hybrid+InScc node、re-derive 被 enqueue 的 IR node=同值不傳播、末 `InvokeCallbacks()`=memory/video）⑥ trace+Time++。加 `StepDriving(int)`/`RunFrameDriving`。(2) `TestRunner` 加 `--engine ir`：`--trace --engine ir` 用 `StepDriving`；`--trace-cmp --engine ir` → `TraceCmpDrive`（snapshot-compare：跑 S1 N 拍快照 → reset → 跑 IR-driving N 拍快照 → diff 每個 observable node）。(3) `WireCore` 的 `RunHandlerChain`/`SetNodeState` 改 internal；不碰 S1 執行期（`DeferRecalc` 預設 off → S1 不變）。`--selftest` 全 PASS（10 method）、`--trace branch_timing/1 --cycles 8` 跟 `9dca67e` 一致、build 0/0（Debug+Release）。**`--trace branch_timing/1 --cycles 8 --engine ir` 的 trace 跟 S1 逐 cycle 完全一致**（reset 序列 + 第一條指令）。**`--trace-cmp branch_timing/1 --cycles 2000 --engine ir`：IR-driven 6705 node（45.5%）/ 5824 in SCC→S1 / rest hybrid→S1 → `✓ NO MISMATCHES over 2000 half-cycles — IR-driven ≡ S1-driven for every observable node. (S2.6 driving-mode equivalence gate PASSED for this ROM.)`**；加碼 `--cycles 10000`（同 ROM）0 mismatch、`instr_test-v5/10-branches --cycles 4000 --engine ir` 0 mismatch；`--trace-cmp`（checking 模式）仍 0 mismatch（IR 覆蓋 85%）。**→ S2.4（IR 直譯器 driving 模式）+ S2.5（hybrid bridge = step ⑤ 的 ProcessQueue）+ S2.6（等價 gate，checking *和* driving 兩版都過）達成**。IR-driving 引擎逐 half-cycle 產出跟 S1 bit-identical 的 observable 狀態（IR 算 ~45% node 的純布林 eval + S1 ProcessQueue 算其餘）。**下個 firing**：(a) 提升 driving coverage 接近 checking 的 85% —— 打斷那 5824 個 SCC node 的環（Stage A 一般化 cross-coupled-NOR-with-load latch；Stage D 用 Prev 打斷 feedback edge）；或 (b) **S3 前哨：`--benchmark <rom> --engine ir` vs S1 量吞吐量**（第一刀因 ProcessQueue 仍 re-derive 被 enqueue 的 IR node 可能沒快多少；S3 才優化讓 ProcessQueue 只碰 hybrid 區）|
| 2026-05-12 | `f81fce4` | **S2.4 firing 11** — S2.3b 殘餘 SCC 處理設計 + Gemini review | driving 模式只直譯 ~45% node、其餘 ~55%（含 5824 個 residual SCC node）丟 S1 → 設計 S2.3b 把那些環打斷。寫設計（`05_S2.4_*.md` 的「firing 11 起：S2.3b」段）：**Stage A2**（接在 `SccModel.Build` 的 Stage A 後）—— 一對 `{a,b}` 若 `di[a].PullDown` 含 `NodeRef(b)` 當 OR-term、`di[b].PullDown` 含 `NodeRef(a)` 當 OR-term、兩邊 `StaticLoad`/`StrongVcc` → 是個 cross-coupled-NOR-with-load latch（74LS373 那種 `q=!(/q | load_q)`）→ `rest_a/rest_b` = 把那個 OR-term 拿掉 → `NextExpr[a]=Mux(rest_a,0,Mux(rest_b,1,Prev(a)))`、`NextExpr[b]=Mux(rest_b,0,Mux(rest_a,1,Prev(b)))`（**對稱設計**，contention 時兩邊都 eval 出 0 ✓、不用區分 S/C；若 `rest` 含 `NodeRef(a/b)` 則跳過交 Stage D）、兩者離開 hybrid。**Stage D**（接在 `IrEngine.BuildEvalOrder` 的 Tarjan 後）—— 對每個 size>1 SCC 在子圖 DFS 找 back-edge `v→M` → `NextExpr[v]` 裡 `NodeRefExpr(M)` 換成 `PrevExpr(M)`（打斷環、v 變序向）→ 重跑 Tarjan、迭代到沒 SCC（上限 10、剩的標 InScc）；blind 打斷（任一條 back-edge、不用 clk0/clk1 互斥）。丟 Gemini（log `..._132255.txt`）—— **「非常精準且具備極高可行性」**，5 條開放問題全確認 + 兩個微調全採用：(1) 只抓 NOR-NOR、不做 NAND-NAND（NMOS 基本閘是 NOR、ROI 低）；(2) **Stage A2 改對稱 model**（不用 `Not(Node(S))` —— contention 時會分歧；對稱兩邊都 `Mux(rest_x,0,Mux(rest_y,1,Prev(x)))`）+ 防禦檢查（`rest` 含 partner 則跳過）；(3) Stage D blind back-edge `Prev` 打斷「安全、不用挑邊、不用 clk0/clk1」（切任一環邊補 Prev = 插 1-tick delay、穩態不變）；(4) Stage A2 於 `SccModel`、Stage D 於 `IrEngine` 的解耦「完全正確」；(5) 713-node 大 SCC = "tangle"（沒打斷的 2-cycle latch 透過雙向 pass/bus 黏在一起 + 2C02 多相位 shift-register）—— Stage A2 拆積木後會碎成中型 SCC、Stage D 砍 back-edge 消滅 → **預期 residual SCC 從 5824 驟降至 0、driving coverage 衝破 80%+**。**下個 firing：實作 S2.3b**（① `SccModel.Build` 加 Stage A2 + selftest case；② `IrEngine.BuildEvalOrder` 的 Tarjan 加 Stage D + 迭代外迴圈；③ 重跑 `--trace-cmp`/`--trace-cmp --engine ir` —— checking 仍 0 mismatch、driving coverage 大升、driving 仍 0 mismatch；④ 順手 `--benchmark --engine ir` vs S1）。本 firing 純文件（無 code 改動 → build/selftest/trace 跟 `c30cd7d` 不變）|
| 2026-05-12 | `c9eafd7` | **S2.4 firing 12** — S2.3b：Stage A2 實作；Stage D 試做後 revert | (1) **Stage A2**（`SccModel.Build` 的 Stage A 後）：一對 `{a,b}`（`a<b`）若 `di[a].PullDown` 的某 OR-term 是 `NodeRef(b)`、`di[b].PullDown` 的某 OR-term 是 `NodeRef(a)`、兩邊 `PullUp` 是 `StaticLoad`/`StrongVcc`、且 `m.NextExpr[a]` 仍引用 `Node(b)` 且 `m.NextExpr[b]` 仍引用 `Node(a)`（活的 `q↔/q` cycle、不是 Stage A 已處理的）→ `rest_a`=PullDown 拿掉 `NodeRef(b)` OR-term（空→`False`）、`rest_b` 同 → 若 `rest` 含 `NodeRef(a/b)` 則跳過 → `NextExpr[a]=Mux(rest_a,False,Mux(rest_b,True,Prev(a)))`、`NextExpr[b]=Mux(rest_b,False,Mux(rest_a,True,Prev(b)))`（對稱）、兩者 `IsSequential=true`/`Hybrid=false`、`RecoveredLatches2++`。helper `CollectOrTerms`/`ReferencesNodeRef`。(2) **Stage D 試做後 revert** —— Tarjan DFS 裡 cut 每條 back-edge（`NodeRef(w)→Prev(w)`）→ 測下來 **checking 模式從 0 變 2091 mismatch**（改了 `NextExpr` —— `clk0_int` 那種每拍都變的 node，`Prev(n160)≠Node(n160)` → checking 比就掛）+ **driving 也掛**（clock divider 的 cycle 不是 pass-through pipeline、IR 抽出來的 `NextExpr[clk0_int]` 本身就錯（toggle 模型化成 clear），`Prev`-cut 一個錯的 model 不會修好 → 整個 PPU 下游 ×1994 mismatch）→ **revert Stage D**，回 firing 10 的 InScc 行為。Stage D 要做需先「分類 cycle 是不是 pure pass-through pipeline（每個 node 都是 `Mux(clk_phase, Node(prev), Hold(self))`）」—— deferred。`--selftest` 全 PASS（10 method）、`--trace branch_timing/1 --cycles 8` 跟 `f81fce4` 一致、build 0/0（Debug+Release）。`--trace-cmp branch_timing/1 --cycles 2000`（checking）→ IR 覆蓋 12529/14737（85.0%）→ **`✓ NO MISMATCHES`**（S2.6 gate 仍過）；residual SCC `2254 SCC / 5705 node`（從 2300/5824 —— Stage A2 救回 ~119 node、最大 SCC 703 從 713）。`--trace-cmp ... --engine ir`（driving）→ IR-driven `6824 node`（**46.3%**，從 45.5%）→ **`✓ NO MISMATCHES`**（S2.6 driving gate 仍過）。**→ S2.3b 的 Stage A2 達成（modest +119 node）；Stage D deferred（需 cycle 分類器）**。**下個 firing**：(a) Stage D 加 cycle 分類器（只 cut pure-pipeline cycle）救回 2C02 shift-register ring；剩的 stateful cycle（clock divider）留 InScc 或回頭把它的 `NextExpr` 抽對；或 (b) **S3 前哨：`--benchmark <rom> --engine ir` vs S1 量吞吐量** |
| 2026-05-12 | `b65efd4` | **S2.4 firing 13** — S3 前哨：`--benchmark --engine ir`（IR-driving vs S1 吞吐量）| `TestRunner.Benchmark` 加 `useIr`（`--benchmark <rom> --engine ir`：`LoadSystem` 後 `IrEngine.Build()`、計時迴圈用 `IrEngine.RunFrameDriving()`、印 IR coverage）；`--frames` 最小值 4→1（快速 benchmark）。不碰 S1 執行期。`dotnet build` 0/0（Debug+Release）、`--selftest` 全 PASS、`--trace branch_timing/1 --cycles 8` 跟 `c9eafd7` 一致。**量到**（`branch_timing/1` 2 frames = 1,369,455 half-cycles = 57,061 6502 cyc，Release）：**S1** 29.7 s（0.07 FPS、~46k step/s）；**IR-driving**（46.3% IR-driven）**232.7 s（0.01 FPS、~5.9k step/s）= ~8× 慢**。→ 第一刀 driving 模式比 S1 慢 ~8×（符合預期 —— 計畫 §4 / Gemini 都標了「S2.4 求對不求快、S3 才優化」）。瓶頸：`StepOneDriving` step 4 對每個值有變的 IR node `SetNodeState(v,nv)+EnqueueNode(v)` → 把 gate fan-out（含一堆 IR node）全 enqueue → step 5 ProcessQueue 對 queue 全跑 `RecalcNode`=`ComputeNodeGroup`（group-BFS）→ 等於把 IR 算對的 node 再 switch-level 重算一遍 → IR eval 變成「S1 全部工作 + 額外開銷」。**→ S2.0~S2.6 都過了 gate（checking 85% / driving 46.3% IR coverage、兩個等價 gate 都過）；S2.3b 的 Stage D deferred（需 cycle 分類器）；driving 模式第一刀比 S1 慢 ~8×（S3 要優化）。下個 firing：S3 開始**——(1) **bridge 瘦身**：step 4 直接寫 `NodeStates[v]`（不 enqueue gate-fanout）、step 5 只 enqueue hybrid+InScc node（+ 值有變且 gates 到 hybrid-相關 transistor 的 IR node）；確認 callback（memory/video）觸發還在；重跑 `--benchmark --engine ir`（看快多少）+ `--trace-cmp --engine ir`（仍 0 mismatch）。(2) 提升 IR coverage（Stage D w/ classifier、clock divider 抽對）。(3) IR eval 攤平成線性指令（S3.1）|
| 2026-05-12 | `99523cd` | **S2.4 firing 14** — S3.0 bridge 瘦身：設計 + Gemini review + 實作試做後 revert | 設計（`05_S2.4_*.md` 的「firing 14 起：S3.0」段）：讓 driving 模式的 `ProcessQueue`（hybrid bridge）跳過 IR-covered non-SCC node 的 `RecalcNode`（IR 已算對、`ComputeNodeGroup` 只會 re-derive 同值 = 純浪費；callback 在另外的 fake `target` node 上、不會被 skip）。改動：`IrEngine.OkToSkipInRecalc[]` + `WireCore.SkipRecalcOf` flag + `ProcessQueue` 裡 `if(SkipRecalcOf?[nn]==true) continue;` + `StepOneDriving` step 4 改 `SetNodeState(v,nv)` only。丟 Gemini（log `..._134915.txt`）—— **「非常精準、邏輯嚴密、不需改、直接實作」**，3 條開放問題全確認（skip 安全；ProcessQueue 跳過法是 sweet spot；預期回 ~par、下個瓶頸是 IR eval AST 遞迴 → 攤平成 instruction array）。**但實作後測下來 driving 模式從 0 mismatch 變大量 mismatch**（CPU 在 reset 序列就崩 —— `S=05` 而非 `FD`、`DB=00` 而非 `24` = reset vector 從 `$FFFC` 讀不回 → memory callback / data path 斷）。root cause（推測）：skip `RecalcNode(v)` —— 但 `RecalcNode(v)` 不只算 `v`、它 `ComputeNodeGroup(v)` 走整個 group + 對每個 group member `SetNodeState`；某些 IR-covered `v` 跟一個 hybrid node / callback-watched node / 板間 buffer 輸出在同一 channel-connected group 裡，`RecalcNode(v)` 是把值傳到那些 member 的途徑 → skip 掉 → 那些 member 變 stale → memory callback 沒拿到正確 address / data path 斷。Gemini（跟我）的「`ComputeNodeGroup(h)` 自己會走到 `m`、`m` 不用 recalc」對 hybrid `h` 成立，但漏了「`v` 跟 callback-target / 跨晶片 buffer 在同一 group」的情況。→ **revert 整個 S3.0 實作**（回 `b65efd4` code state）。`--selftest` 全 PASS、`--trace`/`--trace-cmp`/`--trace-cmp --engine ir` 都跟 `b65efd4` 一致（0 mismatch）。**淨產出**：S3.0 設計 + Gemini review；發現「skip RecalcNode 對某些 IR-covered node 不安全」；記下更保守的修法（**channel-graph connected component 檢查** —— 用 union-find 把所有 channel transistor 當邊算 component，只 skip「component 純 IR-covered（無 hybrid / 無 InScc / 無 callback target）」的 node 的 `RecalcNode`；或**精準 enqueue** —— step 4 直接寫 `NodeStates`、step 5 只 enqueue 變動 IR node 的 gate-fanout 裡的 hybrid/InScc/callback node，而非「全 enqueue 再 skip」）給未來 firing。**下個 firing**：S3.0 修法之一（保守 component 檢查 / 精準 enqueue）；重跑 `--benchmark --engine ir` + `--trace-cmp --engine ir` |
| 2026-05-12 | （本次） | **S2.4 firing 15** — S3.0 修法二（保守 channel-graph component 檢查）試做後也 revert | 接 firing 14：用 union-find 算「channel-graph connected component」（transistor 當邊、但**不**穿過 vcc/vss）→ 只 skip「component 純 IR-covered（無 hybrid / 無 InScc / 無 Input）」的 node 的 `RecalcNode`（這保證 skip 掉的 `v` 的 group 全是 IR 已算對的、不用傳播到任何非-IR member）；step 4 維持 firing 12 的 `SetNodeState(v,nv)+EnqueueNode(v)`（non-skippable 的 `v` 還會被 `RecalcNode`）。**結果：driving 模式還是壞**（`--trace-cmp --engine ir` 大量 mismatch、CPU 在 reset 序列就崩 —— reset vector 讀不回，跟 firing 14 同症狀）→ **revert**（回 `99523cd` code state）。**結論**：「skip `RecalcNode` 對某些 IR-covered node 不安全」成立，但**確切原因比目前理解的深**（連保守 component 判據都壞 → 不是「skip 掉的 `v` group 裡有非-IR member」那麼簡單；也許是 `RecalcNode` 別的 side effect / `EnqueueNode(v)+skip` 跟 ProcessQueue buffer dance 互動 / memory callback 某個沒想到的觸發路徑）。**下次做這個優化先 instrument**：給 driving 模式加 lockstep-diff（每拍比 IR-driven `NodeStates` vs 同拍 S1-driven、印第一個發散 node + `NextExpr` + group 成員 + 被/沒被 skip）→ 找出確切哪個 node stale → 針對性修。`--selftest` 全 PASS、`--trace`/`--trace-cmp`/`--trace-cmp --engine ir` 都跟 `99523cd` 一致（0 mismatch、46.3% IR-driven）。**現狀**：driving 模式正確（兩個等價 gate 都過、46.3% IR coverage）但比 S1 慢 ~8×（瓶頸 = bridge ProcessQueue re-derive 一堆 IR node）；S3.0 方向對但實作要更仔細（先 debug 清楚）。**下個 firing**：(a) 給 driving 模式加 lockstep-diff instrument → debug `SkipRecalcOf` 為何壞 → 針對性修；或 (b) 先做別的 S3 項（IR eval 攤平成 instruction array —— Gemini 說是更大的瓶頸、~50K AST visit/step、攤平後再快 5-10×、且不碰 bridge 那塊脆弱的東西）；或 (c) Stage B/D 提升 IR coverage（減少 InScc/hybrid → bridge 自然變輕）|
| | | | |
