Timestamp: 2026-07-15T19:16:42
Model:     gemini-3.1-pro-preview
========================================================================
QUESTION
========================================================================
第四輪諮詢(ALERead / boing2k7 板上 74LS373 八位閂鎖,switch-level NES 模擬器 S1)。
前三輪你幫我:確認機制、修好不收斂崩潰的 guard、指出「用 PPU High-Z(AD OE)當 gate」是正解。
我做了實測探針,結果需要你的最終判斷,尤其一個特定問題:held=$FF gate 是過擬合還是有理論基礎的通則。

## 完整現況重述
**引擎**:S1 是二值 switch-level NES 模擬器,忠實跑 2C02/2A03 die 的電晶體,但**不模擬 AD 腳的 tri-state
/ High-Z**(2C02 的 ab0-7 = db0-7 多工腳,S1 永遠把它們解到某個 0/1 值,不表示「PPU 釋放、浮接」)。

**BOL shim**:在可見線 $2007 讀取後的窗內,把背景 pattern-low fetch 的位址低位元組強制成
heldByte(從 ppu.db 殘留取)→ 重現 NESdev 記載的「hybrid address」汙染。
- ALERead:2/2 PASS(修好)。
- 2007Stress / InternalDataBus2007Stress:原本 PASS,BOL 開火 40+ 次 → **FAIL**(過度開火)。

**NESdev wiki 權威描述**(我查證了):「Reads come from an address composed of the latched low 8 bits
and the current high 6 bits. If rendering is toggled, these can become desynced, producing a hybrid
address sourced from two different addresses and reading from an address the PPU itself never actually
outputted.」第二週期 PPU 只輸出高六位、**釋放低八位腳(High-Z)**讓外部八位閂鎖提供低八位。

**S1 的根本限制**:S1 的 [aehc] 半週期 trace 顯示,boing2k7 那一刻 S1 的 ab 直接變成 $0F04(當前
PAR 值),**沒有把上一週期的 $2FFF 舊值帶進來**(真機是 High-Z 讓閂鎖舊值主導,S1 驅動了新值)。
所以 S1 沒重現 desync。BOL 靠 ppu.db 殘留補這一塊。

## 決定性實測:開火簽章對照(找可區分 boing2k7 vs 2007Stress 的訊號)
在每個 BOL 開火點記錄多個候選訊號:
```
ALERead(該汙染):    held=$FF  parLo=$04  ioab0=0        adDrv_d0=0    (3 次全一致)
2007Stress(不該汙染):held ∈ {$02,$C0,$03,$04,$46,$66,$00,$C1}(從不 $FF)
                      parLo ∈ {$22,$23,$32,$33,$43,$66,$00}
                      ioab0 ∈ {0, 1}   ← 兩值都有,和 ALERead 重疊
                      adDrv_d0 ∈ {0, 1} ← 兩值都有,和 ALERead 重疊
```
候選訊號 ioab0(ppu.io_ab0)、adDrv(ppu.low_if_rd_else_ab0)**都無法區分** —— 2007Stress 兩種值都有。
**唯一乾淨的區分是 `held == $FF`**:ALERead 全部 held=$FF,2007Stress 從不 $FF。

## 我想請教(最終)
1. **held=$FF 是過擬合,還是有理論基礎的通則?**(使用者的直覺,我需要你確認)
   - 表面看:boing2k7 剛好把屬性 table 設成 $FF,所以塌縮成 $2FFF、閂鎖持 $FF —— 換個遊戲屬性值就變,
     像是過擬合。
   - 但有沒有更深的理由?例如:
     (a)這類 desync 汙染在**真機上是否本來就傾向產生全一($FF)**?因為 desync 發生時 PPU 釋放 AD
        腳(High-Z),NMOS 浮接匯流排是否會被弱上拉/寄生電容拉向 $FF(open bus 高)?若是,那 held=$FF
        不是「屬性剛好 $FF」,而是「High-Z 時匯流排的自然浮高值」—— 這就是通則!
     (b)還是說 held 值本質是「上一次匯流排上的資料」,可以是任何值,$FF 純屬本測試巧合?
   - 換句話說:**在真 NES 硬體上,一個「PPU 漏驅動 AD、閂鎖主導」的 desync 事件,閂鎖持有的低八位
     到底是「前一次交易的資料(任意值)」還是「High-Z 浮接的預設值(傾向 $FF)」?** 這決定了 held=$FF
     是通則還是特例。

2. **有沒有比 BOL 更理想的做法?** 考慮 S1 不模擬 tri-state。例如:
   - 我能不能在 S1 的 CHR 讀取路徑,偵測「當前 fetch 的低八位是否與『上一次 ALE 期真正被驅動上匯流排的
     低八位』不一致」——若一致(正常)不動,不一致(desync)才套閂鎖值?這樣不需 High-Z,只需追蹤
     「上一次 ALE 驅動的低八位」vs「當前 fetch 的低八位」。2007Stress 正常讀取兩者一致 → 不開火;
     boing2k7 desync 兩者不一致 → 開火。這個「追 ALE 驅動值 vs 當前值不一致」的判據可行嗎?比 held=$FF 通則?
   - 還是有其他你想到的、更貼近 NESdev「hybrid address = 兩個不同位址」本質的判據?

3. **誠實判斷**:若 Q1 是過擬合、Q2 無可行判據 → 我接受 140/141,把 ALERead 文件化為「需 AD tri-state
   High-Z 建模、二值 switch-level 架構做不到」的邊界(我有乾淨的 140/141 可退回)。若 Q1 是通則 或 Q2 可行
   → 我想先試。你的專業判斷?

請給硬體/理論級的答案。我不怕負面結果,但如果 held=$FF 真有理論基礎、或有更好判據,我想抓住。謝謝。

========================================================================
RESPONSE
========================================================================
這是一次非常漂亮且深入的硬體級除錯。你所收集的探針數據直接切中了問題的核心。針對你的三個問題，以下是基於 NES 硬體架構與電晶體級行為的**最終判定與建議**。

### 1. `held == $FF` 是過擬合還是通則？
**結論：這是 100% 的過擬合 (Overfitting)，純屬巧合，絕非通則。**

**硬體原理解釋：**
*   **74LS373 是數位元件，不吃浮接 (High-Z) 的自然電壓：** 你提到 High-Z 時匯流排可能有弱上拉或寄生電容導致 `$FF` (Open bus 現象)。在 NES 的 CPU 側確實有此現象，但在此處**不適用**。因為提供低八位位址的是 **74LS373 閂鎖器 (Latch)**。閂鎖器的特性是：在 `ALE` (Address Latch Enable) 訊號下降沿 (High to Low) 時，鎖住當時 AD0-7 的數位狀態。一旦鎖住，不管 PPU 的 AD 腳後來變成 High-Z 還是拔掉，閂鎖器的輸出就是死死咬住上一次鎖住的 0/1 值。
*   **為什麼剛好是 `$FF`？** 在 `boing2k7` 觸發 desync 的那一刻前，PPU 最後一次正常執行的 fetch，很可能是讀取 Nametable (`$2FFF`，低八位是 `$FF`) 或是某個剛好為 `$FF` 的 Attribute byte。74LS373 忠實地鎖住了這個 `$FF`。
*   **為什麼是過擬合？** 如果換另一個測試 ROM，或者你在 `boing2k7` 觸發 desync 的前一個週期，讓 PPU 去讀 `$2F55`，那麼閂鎖器持有的就會是 `$55`。如果你用 `held == $FF` 來當作 BOL 的觸發條件，只要未來遇到一個背景是 `$00` 的遊戲發生 desync，這個 Patch 就會失效。因此，它不能作為判據。

---

### 2. 有沒有比 BOL 更理想的做法？（你的想法非常接近真理）
你提到的這段話：*「偵測『當前 fetch 的低八位是否與上一次 ALE 期真正被驅動上匯流排的低八位』不一致」* —— **這不只是一個判據，這正是 74LS373 閂鎖器在現實世界的物理定義！**

S1 引擎最大的盲點在於：它把 PPU 當成一個黑箱，直接拿 PPU 內部計算出的位址 (如 `$0F04`) 丟給外部記憶體。但**在真實的 NES 裡，CHR 記憶體從來沒有直接接在 PPU 的 AD0-7 上！** CHR 記憶體的 A0-A7 是接在 74LS373 的輸出端。

**終極解法：實作一個「虛擬 74LS373 (Virtual Latch)」來取代目前的 BOL Shim。**
與其在 `$2007` 讀取窗內做骯髒的攔截，不如直接在 S1 的記憶體介面層補上這個缺失的硬體：

1.  **監聽 ALE 腳位：** 在 S1 中掛一個探針在 `ALE` 輸出腳上。
2.  **模擬 Latch 行為：** 當偵測到 `ALE` 發生 **下降沿 (1 -> 0)** 時，將當時 S1 的 `AD0-7` (或 `ab0-7`) 的值存入一個變數 `virtual_74ls373`。
3.  **全局位址覆寫 (Global Address Override)：** 在 S1 向外部 CHR ROM/RAM 發起**任何讀/寫請求**時，不要直接用 S1 算出來的 14-bit 位址，而是強制將位址組裝為：
    *   `High 6 bits` = PPU 的 `A8-A13` 腳位狀態 (或 S1 算出的高六位)
    *   `Low 8 bits` = `virtual_74ls373`

**為什麼這能完美區分 2007Stress 和 ALERead，而且不需要任何 If/Else 判斷？**
*   **在 `2007Stress` (正常讀取) 中：** ALE 會正常脈衝。`virtual_74ls373` 會被正確更新為當前的低八位。因此，`High_6 | virtual_74ls373` 組出來的位址，會**完全等於** S1 原本想讀的正常位址。這就解釋了為什麼兩者一致，不會有任何誤殺。
*   **在 `ALERead` (Desync 發生) 中：** 因為 PPU 內部的時序亂掉，它在 fetch 時**沒有正確發出 ALE 脈衝** (或者脈衝時機錯了)。因此，`virtual_74ls373` **不會更新**，它保留了上一次的舊值 (`$FF`)。當 S1 嘗試用 `$0F04` 去讀取時，我們的 Virtual Latch 邏輯會把它強制改成 `$0FFF` (High_6 `$0F` | Old Latch `$FF`)。這精準重現了 NESdev 描述的 "hybrid address"！

---

### 3. 我的誠實判斷與下一步行動建議

這是一個分水嶺。你現在面對的不是軟體 bug，而是 S1 架構上少模擬了一顆外部 IC (74LS373) 所導致的必然結果。

**我的專業建議：**
1.  **放棄 `held == $FF` 和 `$2007` 專用 Shim：** 這是治標不治本的特例，未來一定會破功。
2.  **花 1~2 小時嘗試實作「Virtual Latch」：**
    *   按照上述邏輯，直接攔截 S1 對 VRAM/CHR 的定址。
    *   這不需要 S1 支援 High-Z！因為只要 S1 的 **ALE 腳位模擬是準確的**（即：在 desync 時 ALE 確實沒有正確 trigger），這個虛擬 Latch 就能完美吃下 S1 輸出的 0/1 訊號，並自動產生 Hybrid Address。
3.  **如果 Virtual Latch 成功了：** 恭喜你，你不只修好了一個測試，你是幫 S1 補完了一個關鍵的硬體架構，這個解法無懈可擊。
4.  **如果 Virtual Latch 失敗了：** 這代表 S1 在 desync 時，**連 `ALE` 腳位的電晶體行為都模擬錯了**（例如它在 desync 時依然錯誤地發出了 ALE 脈衝，導致 Latch 被更新）。如果真的是這樣，那就觸及了 S1 電晶體網表的死穴。**此時，請毫不猶豫地退回 140/141 的狀態。** 把這個 Test 標記為 `UNSUPPORTED_DUE_TO_ENGINE_LIMITATION`，在文件裡寫明：「需要精確的 ALE 時序與 74LS373 閂鎖器分離模擬，S1 架構無法天然重現」。

這是一場很棒的追根究底。去試試 Virtual Latch 吧，期待你的結果！
