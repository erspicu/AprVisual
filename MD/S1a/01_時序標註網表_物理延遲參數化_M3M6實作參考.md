# 時序標註網表(Timing-Annotated Netlist)—— 物理延遲參數化實作參考

**日期**:2026-07-16。**出處**:使用者提供的外部諮詢對話(CPU↔PPU 延遲物理成因 → SPICE 定位 →
時序標註網表提案),經本專案戰役實測**逐點校驗**後整理。
**定位**:S1a **M3(RC 傳播延遲)+ M6(相位)** 的資料驅動實作策略;工業界對應物 = **SDF
(Standard Delay Format)** 掛在主網表旁的時序標註。
**狀態**:參考設計。**S1a 實作仍待使用者明確啟動,勿自行開工**(見 [[s1a-analog-refactor-on-hold]])。

---

## 一、諮詢主張 vs 本專案實測(校驗表)

| 諮詢主張 | 本專案證據 | 判定 |
|---|---|---|
| 開關級自動繼承**架構層管線延遲**($2007 read buffer、7-cycle reset 甦醒、DMA even/odd 對齊) | 全部網表自跑,零 hack:$2007 dummy-read 語意天生正確;reset 後 7 週期 + $FFFC 向量自動發生 | ✅ 證實 |
| **物理層傳播延遲**是 0/1 引擎唯一遺失的東西 | 與戰役分類完全吻合:A4 類 + DMA-abort($4015→sequencer 晶片**內部**傳播)+ M6 三顆(dot-339 / even_odd / ALERead = CPU→PPU 早 24hc) | ✅ 證實 |
| RESET 需外部模擬 RC(壓 /RES → 時間到放開 → 網表自跑 7 週期) | S1 已實作:reset-hold(`--reset-hold-extra`),netlist 自己跳完甦醒舞 | ✅ 已在做 |
| SPICE 全類比可完美解,但 1 frame 要數小時~數天 | 與 thesis / WebSite/study.html 的定位一致;不走 | ✅ 同結論 |
| 傳播延遲「主要」在晶片間;晶片內部也無所不在 | DMA-abort 就是**晶片內部**傳播延遲(2A03 內 $4015→DMA 閘門);ALU ripple-carry 對應 AluLatchShim 的賽跑 | ✅ 證實 |
| 標註格式:**per-node** `propagation_delay_hc` | **太粗,需修正**(見 §三修正 1) | ⚠️ 修正 |
| 引擎切勿用 priority queue,要平行陣列 + 邊界掃描 | 與反模式總綱一致(加成本必輸);現有 shim 鏈已是手工版證據 | ✅ 採納 |

**一句話**:諮詢的物理敘事和引擎定位全對;唯一要動手術的是「標註掛在哪一層」——
我們的戰役證明**事件層**(access event)比**節點層**(node)才是可實作的正確粒度。

---

## 二、核心提案:sidecar 時序標註(採納)

主網表(Visual6502 衍生 .js)**一個位元組都不動**(licence / 上游對齊),物理參數放**副檔 sidecar**,
引擎載入期合併 —— 與既有 `data/system-def/2a03/PATCHES.md`(E 類網表補丁)同一哲學:
**資料驅動,引擎裡永遠不出現 `if (node_id == 1042)` 這種 per-node hack。**

```jsonc
// data/system-def/analog-annotations.json(示意)
{
  "schema": "aprvisual-analog-annotations/1",

  // 事件級:CPU→PPU 介面延遲(M3×M6;修正 1 的正確粒度)
  "cpu_ppu_access_delays": [
    { "reg": 7, "rw": "R", "delay_hc": 24, "mode": "capture-replay",
      "note": "$2007 read ReadALE;ALERead 實證(mux 原型)" },
    { "reg": 1, "rw": "W", "delay_hc": 24, "mode": "downstream-clamp",
      "note": "$2001 render-enable;dot-339 實證" },
    { "reg": 2, "rw": "R", "delay_hc": 0,
      "note": "⚠️ $2002 讀取不可延遲 —— NMI golden alignment(M6 非對稱鐵律)" }
  ],

  // 節點級:只放事件層蓋不到的
  "nodes": [
    { "name": "cpu.dma_4015_ctrl_path", "delay_rise_hc": 6,
      "note": "晶片內部傳播;DMA-abort 家族" },
    { "name": "ppu.io_db[7:0]", "capacitance": "bus-hold",
      "note": "open-bus 殘留 = last-transferred-byte(M2)" },
    { "name": "cpu.pcm_latch", "transparent_latch": true,
      "note": "DMC 關門沿賽跑(M4)" }
  ]
}
```

---

## 三、本專案實測對諮詢格式的三個修正(價值核心,實作前必讀)

### 修正 1:node-level 太粗 → 必須是「存取事件」級
**同一顆 `ppu.io_ce` 同時服務 $2002 和 $2007**:$2007 存取要延遲 24hc,$2002 讀取**絕對不能延遲**
(推遲它就打壞 NMI-edge 8 顆 —— M6 戰役實測)。單一節點掛一個 `delay_hc` 無法表達這種**非對稱**;
正確單位 = `(暫存器選擇, 讀/寫)` 的**存取事件**。這是 M6「非對稱介面延遲」的資料化。

### 修正 2:rise/fall 可實作性不對稱(GND>VCC 牆)
群解析優先序 **GND > VCC/pull-up > 外部驅動** 造成:
- **延遲上升沿:通用可行** —— 自然上升時 InstClampLow 壓住 N hc,到期 InstRelease,網表自己把它拉高
  (release 不是 assert,沒有牆)。
- **延遲下降沿:撞牆** —— 「多撐高 N hc」= assert-high 對抗網表 drive-low,必敗(§五-c 實證,
  P27 / read_2007_trigger 延遲全死在這)。兩條活路,標註必須**聲明用哪條**:
  `fall_via: "complement:<node>"`(鉗互補節點低 = invert-the-force)或
  `fall_via: "event-replay"`(把「延遲信號」轉成「延遲存取事件」—— ALERead mux 的關鍵轉向)。

### 修正 3:延遲存取需要「值的載體」→ split/replay 是圖變更,必須 opt-in 分層
延遲一個 CPU→PPU 存取時,**CPU 早就放開匯流排了**(io_ab 在 h=227 掉 0、io_db 換內容)——
所以要嘛**捕捉-重播完整事件**(含 io_ab/io_db/rw 值),要嘛**載入期切線 + 軟體 relay**
(ALERead mux 的 node-split)。切線是載入期圖變更 → **D 類對齊彩票重擲風險**([[socket-pattern-dut-immutability]]);
所以標註啟用必須是 **opt-in 層(test-mode)**,金 checksum / benchmark 路徑**零觸碰**
(mux 的 `ALEREAD_MUX` env 已示範這個紀律)。

---

## 四、引擎實作模式(諮詢正確,採納;與現有 shim 鏈同構)

**禁忌**:離散事件模擬的 min-heap / priority queue —— O(logN) + cache miss,直接毀掉 O(1) 查表熱迴圈
(反模式總綱「加成本必輸」)。

**正解**:載入期把標註編譯成 cache-friendly 平行陣列,**StepCycle 邊界**(不是 settle 內)輕量掃描
**只有被標註的那 ~1% 節點/事件**:

```csharp
// 載入期(一次):annotations → 平行陣列
int[] delayedNodes;        // 只含標註節點(緊湊)
int[] delayThresholdHc;    // 各自的延遲量
long[] changeTimestamp;    // 轉態起點 hc
byte[] targetState;        // 等待生效的目標值

// StepCycle 尾端(每 hc,現有 shim 鏈同位置):
for (int i = 0; i < delayedCount; i++)
    if (Time - changeTimestamp[i] >= delayThresholdHc[i]) FireDelayed(i);
```

**現有 shim 鏈就是這個模式的手工版證據**:`DmcLatchShimStep` / `Dot339DelayStep` / `AleReadMuxStep`
都掛在 StepCycle 尾端、每 hc 一個 flag check,對 ~80-142K hc/s 的熱迴圈**量測無感**。
通用化 = 把「每個場景手寫一個 ShimStep」換成「一個 generic step 吃資料表」。

---

## 五、現有 shim → 標註對應表(通用化路線圖)

| 現有 shim(手工) | 標註形式(資料) | 機制歸屬 |
|---|---|---|
| PpuWriteDelay(even_odd,16hc 窄窗) | access_delay {reg=1, W, 16hc} | M3×M6 |
| PpuWriteDelayGlobal(dot-339,24hc) | access_delay {reg=1, W, 24hc, downstream-clamp} | M3 |
| **AleReadMux(ALERead,24hc + freeze)** | access_delay {reg=7, R, 24hc, capture-replay} | M3×M6×M4 |
| OpenBusShim(last-transferred-byte) | nodes: io_db bus-hold 電容 | M2 |
| DlShim(DL φ2 透明) | transparent_latch {DL/notidl, φ2} | M4 |
| DmcLatchShim / AluLatchShim / FrameIrqShim | transparent_latch / edge-capture 標記 | M4 |
| OamBlankEdgeShim(關渲染邊沿) | edge-effect 標記 | M4 |
| Dmc4015AbortShim($4015→sequencer) | nodes: delay_rise_hc(**晶片內部**傳播) | M3 |
| PowerUpStateShim | 上電狀態標註 | M6/M7 |
| LxaMagicShim($AB 強度競爭) | **標註幫不上** —— 需 M1 電導加權 | M1 |

---

## 六、校準來源與實作優先序(S1a 啟動時)

**已量測的物理參數**(不是猜的,全部有測試背書):
- CPU→PPU 介面延遲 **24hc**(dot-339 / even_odd / ALERead 三顆獨立收束 —— M6 統一假說的實證)
- even_odd 窄窗 **16hc**;dot-339 rise/fall **16/18hc** 非對稱(NMOS 上拉慢/下拉快)
- BGSerialIn reload 邊界 **16hc**(2026-07-17 攻破,第五個錨點)
- Escape-1 的 **~1.1% 不可約類比活動**清單 = 標註目標集的第一近似(S2 的分析價值變現)

**幾何物理先驗(2026-07-17 Gemini 諮詢 a_geometry_delay,把 P5 升級成「錨點回歸的分級器」)**:
- **可行性**:hc 粒度(23.3ns)下 Elmore 一階即可分級 {<0.5hc 忽略 / 1-2hc / 2-4hc / Pad>4hc};
  1982 NMOS 單級 2-5ns、重載路徑 20-50ns → 抓數量級穩。前例 = Berkeley 1983 Crystal/TV。
- **配方**(Mead & Conway 1980):Cox ~1.0 fF/µm²(扇出主力)、poly 0.04 / metal 0.03;
  sheet R:poly 15-50 Ω/sq(延遲殺手)、diff 10-20、metal≈0;Ron:下拉 ~10k、depletion 上拉
  ~40k(4:1)、pass-gate 10-15k。squares 用 bbox 長寬比代理;扇出 C = 下游器件 bbox × Cox。
- **校準**:跨晶片 24hc 錨點**隔離**(其中 ~15-18hc 是 pad/封裝/板線,幾何沒有 → pad 網掛常數);
  晶片內錨點(16hc×2)回歸全域 α;16/18 校 depletion/enhancement 比 β。
- ⚠️ **16/18 rise/fall 方向與 NMOS 天性(rise 應慢 4×)相反 → 量測節點與驅動源間疑有奇數次反相
  (inversion parity),或該網是 super-buffer —— 可立即驗證的預測**。
- **兩個系統性陷阱**:pass-gate 串聯鏈 N² 效應(辨識 S-D 鏈套 N(N+1)/2·RC,否則嚴重低估)、
  bootstrap 節點(RC 高估)。fringe/via/溫度在 hc 級可無視。
- **甜蜜點**:per-net τ script → Binner → sidecar 初稿 → **測試只驗不校**;90% 自動入槽,
  剩 10%(bootstrap/super-buffer)人工。

**優先序**:
1. **Phase A:M6 事件級 CPU→PPU 延遲** —— 拆掉 PpuWriteDelay / PpuWriteDelayGlobal / AleReadMux 閘窗,
   三合一成 `cpu_ppu_access_delays` 表。**這一步同時正式解除 ALERead 的過擬合**(見 §七)。
2. **Phase B:M2 電容資料化** —— OpenBus shim 的 bus-hold 變標註。
3. **Phase C:M4 透明閂鎖標記** —— DMC/ALU/FrameIrq/DL 家族。
4. 每 phase 驗收:金 checksum 不變(標註 off)+ AC 141 / nes-test-roms 147 不退步(標註 on)。

---

## 七、與 ALERead mux 的關係(單點原型 → 通則 = 去過擬合)

**mux = 本架構在單一事件 `(reg=$2007, rw=R)` 上的手工原型。** 它已經實證了核心科學命題:
「把存取事件延遲到物理正確的時間,網表自己跑出硬體行為($FF 捕捉、$0FFF 幻影、sprite-0 hit)」。

mux 目前的過擬合(v=3 / h∈[220,226] 閘窗)是**實作副作用逼出來的**,不是物理需要:
被吞掉的讀取會回傳錯值給 CPU(只在值被丟棄的場景敢開火)。通用化條件 = **完整捕捉-重播含 io_db
讀值路徑**(CPU 照常拿到 buffer 值,PPU 側效應延後 24hc)—— 做到這一步,延遲對**所有** $2007 存取
都成立,閘窗即可移除,原版 boing2k7.nes 的彈跳 artifact 也會自然重現。

**同時解答總綱 M3 的開放問題**:「ALERead 可能不是 net RC 延遲能修的」—— 錯,mux 已實證
ALERead 正是介面延遲問題,M3×M6 事件級延遲可修,不需要獨立的相位校準機制。

---

---

# 偵測篇:標註目標怎麼找(六 Pattern + 封閉分類,經 141+147 兩戰役驗證)

> 2026-07-16 方法論討論收斂(ALERead 攻破後),回答三個遞進問題:(a) 有沒有結構 pattern 能主動
> 找出「需要傳播延遲處理」的雷點?(b) 可否程式化?(c) 兩戰役實際處理的所有雷是否全落在分類內?
> (→ 是,封閉)。掃描器均未實作;實作屬 S1a Phase 0,待使用者啟動。

## 八、傳播延遲家族:六個結構指紋(P1–P6)

### P1 閂鎖賽跑(晶片內最大宗;5+ 顆 shim)
**指紋**:pass-gate 閂鎖(儲存節點唯一驅動路徑經時脈控制的 pass 電晶體),且**致能錐與資料錐共享
支撐** —— 同一觸發事件既關門又改資料,勝負本由傳播延遲決定;零延遲 settle 一律讓新值 ripple
穿過關閉中的門。
**案例**:DmcLatch(t14402 資料沿 vs apu_clk1 關門)、AluLatch(匯流排崩塌 vs SBADD/DBADD 關門)、
DL(φ2 透明)、Dbl2007(緩衝推進,量測 1–7hc)、OamDmaPpuBus($2004 資料 hold 過 /WE)。
**靜態掃**:載入期列舉 pass-gate latch(結構可列舉;mixed-signal M4 計畫同款)→ 檢查 enable 錐 ∩
data 錐(**S2 WireCore.Extract/Cones 的錐計算直接重用**)。注意過濾平凡交集(萬物支撐皆含 clk)。
**動態掃**:settle 內偵測器 ——「pass gate 本波關閉 && 其源側值本波翻轉」→ 記站點、按頻率排名。
早裝的話 DmcLatch/AluLatch 會在測試抓到之前浮出。

### P2 跨晶片取樣器(M6 相位家族;4 顆)
**指紋**:跨 die 邊界的網(清單=nes-001 connections,十幾條)餵進**計數器比較器錐**(下游邏輯
同時吃 hpos/vpos 位元,算「counter==K && 信號」)。介面延遲被抹零 → 效應早 N dot。
**案例**:dot-339($2001→rendering enable)、even_odd(~1-dot 整合偏移)、BGSerialIn(dot%8==7
shifter-load 邊界)、ALERead(io_ce→read_2007_trigger,24hc)。
**掃法**:介面網下游 BFS,支撐含計數器位元即中。全自動,清單小,最乾淨。

### P3 中途廢止(1 顆)
**指紋**:非同步控制寫入 kill 一個**進行中的多週期序列器**;硬體上 kill 信號走內部長路徑,
序列器多退一步;零延遲瞬殺。
**案例**:Dmc4015Abort($4015 disable → DMA sequencer,retire 路徑撐 ~3 cycle)。
**掃法**:靜態偵測 FSM(閂鎖圖 SCC)可做但噪;實用解=動態 ——「規律多 hc 節奏的節點群 + 暫存器
寫入 strobe 落在節奏中段」。**出候選不出判決**(廢止行為的正確答案需 oracle)。

### P4 類比回授迴圈(2 顆)
**指紋**:傳導圖裡經 pass gate / 匯流排 / 行為層回呼的組合迴圈(輸出經讀取路徑繞回輸入)。
**案例**:ALERead 的 74LS373 ALE+/RD 回授、CHR 回授 guard(PpuAleReadFeedbackShim)。
**掃法**:環偵測 —— `HasNonTrivialRomFeedbackCycle` 的 Floyd 雛形**已存在**,擴大範圍即可。
需過濾平凡 SR latch 環(每顆閂鎖都是環);限縮到「跨 pass-gate 同相位開啟 / 經匯流排+回呼」的環。

### P5 幾何 RC 排名(排序器,非判定器)
**指紋**:真實傳播延遲 ≈ 線長 × 扇出電容。segdefs 每 segment 帶 die 上**多邊形座標**,
目前解析後直接丟棄(Parse.cs「we don't keep the polygons」)—— 免費資料。
**掃法**:載入期算每網 bounding-box 跨距(線長代理)+ 總面積(電容代理)+ 扇出 → top-N
「物理上最慢的網」。這就是 EDA RC extraction 的思路。2026-07-14 Gemini 諮詢的面積電容代理
當年因會動 checksum 被否決 —— **當純診斷排名用是零風險**。
**只出排名不出判決**:替 P1–P4 的候選排優先序。

### P6 毛刺捕捉(2 顆;初版分類漏列,完整性檢查時補上)
**指紋**:settle 中途的**瞬態毛刺被儲存元件咬住** —— SR latch 被一個「到靜止時已消失」的脈衝
誤觸發。硬體的慣性延遲吞掉奈秒毛刺;零延遲邏輯照傳。與 P1 的差異:P1 是關門沿 vs 資料的賽跑,
P6 是**根本不該存在的瞬態**被記憶。
**案例**:FrameIrq 誤清(w4017 波 × apu_clk1 沿翻 RS 對;「同波瞬態家族」)、OamBlankEdge
(關渲染邊沿的寄生 OAM 寫入脈衝)。
**掃法**:動態 ——「儲存元件的 set/reset 輸入在單一 settle 內脈衝後歸位」計數器 + 節點單波
多次翻轉審計。全自動。

## 九、可程式化程度(誠實分層)

| Pattern | 靜態 | 動態 | 現成基礎 | 備註 |
|---|---|---|---|---|
| P1 閂鎖賽跑 | ✅ 全自動 | ✅ 全自動 | S2 Extract/Cones 錐機器 | 需過濾平凡 clk 交集 |
| P2 跨晶片取樣 | ✅ 全自動 | ✅ | connections 清單 + hpos/vpos 節點名 | 最乾淨 |
| P3 中途廢止 | ⚠️ 半自動(FSM 噪)| ✅ 出候選 | — | 判決需 oracle |
| P4 回授迴圈 | ✅ 全自動 | — | Floyd 雛形已有 | 需過濾平凡 SR 環 |
| P5 幾何 RC | ✅ 全自動 | — | segdefs 多邊形(撿回即可)| 排名非判定 |
| P6 毛刺捕捉 | — | ✅ 全自動 | [halt]/under-settle 儀器經驗 | — |

**實務漏斗**(避免靜態掃出幾千假陽性):
```
靜態列舉(P1/P2/P4)→ 動態計數(真 ROM 跑,開火頻率排名)
→ P5 物理排序 → 可觀察性過濾(下游錐是否達暫存器/RAM/輸出腳;S2 Cones 重用)
→ [aehc] hc 顯微鏡逐顆確認
```
**Escape-1 的 ~1.1% 不可約類比活動清單 = 現成第一近似**(S2 投資在此變現)。

**根本極限:候選 ≠ 真雷。** 真雷要 (1) 有程式碼踩到該時序、(2) 硬體行為與我們不同 ——
而 (2) 的答案**不在網表裡**(網表沒有時序,這正是問題本身)。判定靠 oracle,但 oracle 比對
本身也可程式化:**AprNes lockstep diff**(calibration_ref.json 基建已半套)、**S2 Dynamic Miter**
(現成的兩模型自動等價比對框架)。
**自動化不了的殘渣只有一種**:連硬體 ground truth 都無文件的角落(boing2k7 作者自己寫
"I'm not sure why it wasn't updated")—— 只能真機量測。

```
候選偵測(P1–P6)      → 100% 可程式化(load-time pass + 儀器化跑)
候選排序(P5+頻率)    → 100% 可程式化
真雷判定              → oracle diff,~90% 可程式化(AprNes lockstep / Dynamic Miter)
最後殘渣              → 硬體無文件角落,只能實機量測
```

## 十、範圍外五家族(傳播延遲之外的雷)

| 家族 | 案例 | 偵測把手 | 自動化程度 |
|---|---|---|---|
| **M2 電荷** | OpenBus last-transferred-byte、io_db decay、OAM 動態 cell | 「以 hold-previous 解析的浮接群 + 下游有消費者」動態計數 | ✅ 全自動 |
| **M1 強度** | LXA $AB magic(ratioed 對抗) | 「同群同時有非電源軌 drive-high 與 drive-low」競爭計數器 | 站點自動;**數值不可推導**(W/L 不在網表)→ oracle/實機 |
| **M6/M7 初態與相位** | PowerUpState(P=$34)、power_up_palette、reset-hold K=1、needsJoypad 圖變更 D 類抽籤 | 不是掃網表 —— 是**參數維度**:掃 4 相位 / 上電表 + oracle 對答案 | 掃描自動;正解靠 oracle(blargg golden alignment) |
| **E 類資料缺陷** | r4015 缺 a1 下拉管、u7/u8 tie 極性(浮接 TTL 應讀高) | 規律性/對稱性檢查給弱提示;實務靠 oracle 分歧回溯 | 最弱 |
| **閘級模組語彙不匹配** | CD4021 手把(pslatch 族):閂鎖 pass-gate 反向驅動輸入,GND 恆勝,「放開的按鍵永遠寫不進去」→ **結構性不可驅動** | **看零件 def 即知**(known-by-construction),零 discovery 成本 | 唯一修法=整模組行為層化(S1a M5) |

規律:**越靠近「結構」自動化越強;越靠近「矽的物理參數與真值」越依賴 oracle。**
網表給連接性;時序、強度、初態、勘誤四樣本來就不在網表裡(thesis 的分界)。

## 十一、兩戰役完整對照(封閉性驗證)

### AC 141 戰役
| 雷 | 家族 |
|---|---|
| DmcLatch / AluLatch / DL / Dbl2007 / OamDmaPpuBus | P1 |
| dot-339 / BGSerialIn / ALERead(+P4 回授) | P2 |
| Dmc4015Abort(ExplicitDMAAbort 16/16) | P3 |
| CHR 回授 guard | P4 |
| FrameIrq / OamBlankEdge | P6 |
| OpenBus last-byte + DL 窗鉗 | M2 |
| LXA magic | M1 |
| PowerUpState、K=1 | M6/M7 |
| r4015 補管(APURegActivation err6) | E |

### nes-test-roms 147 戰役(修復總表:MD/testrom/00 §3)
| 雷 | 家族 |
|---|---|
| DMC latch(+4)、ALU latch、Dbl2007(double_2007_read) | P1(發源地) |
| even_odd(~1-dot)、read_buffer #67 | P2 |
| FrameIrq(同波瞬態家族第三例,140/5 那筆) | P6 |
| LXA magic(+3 的一部分) | M1 |
| io_db decay shim | M2 |
| K=1 相位(NMI-edge 8 顆)、power_up_palette、registers P=$34 | M6/M7 |
| needsJoypad 載入期圖變更抽籤(§2.6 探針效應 → per-test 紀律) | M7/D 類 |
| exec_space_apu tie 極性 + 冷埠 bit0 | E |
| 行為層手把(CD4021 四測 + dma_4016_read) | 語彙不匹配 |

### 不屬於「雷」的兩類(誠實邊界)
- **天然湧現反例**:dma_4016_read —— 「DMA 雙重時脈損毀由真實匯流排流量天然湧現,零專門修正」。
  不是每顆疑難都是雷;有的等真實流量到位就自己對了。
- **忠實偏差(判定不修)**:cpu_dummy_writes_oam(147 唯一殘留)—— 引擎忠於網表、與測試預期歧異,
  文件化為證據卷宗。分類學上掛在 E 類 oracle 歧異邊界,但處置是「不處置」。

**結論:兩戰役 288 顆測試、~25 顆處理過的雷,無一落在分類圖之外 —— 分類封閉。**

```
P1–P6 傳播延遲六 pattern(可全自動掃)
+ M2 電荷 / M1 強度 / M6M7 初態相位 / E 資料缺陷 / 閘級模組語彙不匹配(五個範圍外家族)
+ 忠實偏差(判定不修)+ 天然湧現(不是雷)
```

## 十二、S1a Phase 0 規格草案(偵測 pass)

S1a 開工時,第一步不是寫機制,是跑**偵測 pass** 產出標註目標清單:
1. P1/P2/P4 靜態掃描器(重用 S2 錐機器 + Floyd)→ 候選集。
2. P5 幾何排名(Parse.cs 保留 polygons,一次離線計算)→ 排序。
3. 儀器化跑(P1 動態、P3 候選、P6 審計、M2 浮接計數、M1 競爭計數)於代表性 ROM 集 → 開火頻率。
4. 可觀察性過濾(S2 Cones)→ 修剪。
5. AprNes lockstep / Dynamic Miter 分歧確認 → 真雷清單 → 填 §二的標註 sidecar。

已知答案可直接預填:§十一兩張對照表的 ~25 顆(機制、節點、量測常數全在各 shim 註解與知識庫)。

---

## 相關
- `MD/S1a/00_S1a類比重構設計總綱.md`(M1-M7;本文是 M3/M6 的實作策略 + 偵測方法論)
- `MD/toDoNext4/202607150004-M6統一相位-CPU-PPU介面延遲-實作計畫.md`(捕捉-重播 pipeline 原始設計)
- `MD/testrom/00_測試修復知識庫_總綱.md`(147 戰役修復總表、§2.4 四質地、§2.6 探針效應)
- [[aleread-board-octal-latch-boss]](mux 攻破記錄 + 唯一驗證校準)、[[dot339-rendering-propagation-delay]]、
  [[s2-levers-exhausted-pivot-analysis]](Escape-1 類比島清單)、[[instrument-probe-effect-instclamp]]
- branch `aleread-ioce-mux`:`89370de`(node-split + ale-freeze)、`6971518`(緊閘)、`2d27d5e`(freeze 窗)
