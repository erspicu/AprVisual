# S1 移植任務清單

把 `ref/metalnes-main` 的開關層級引擎逐步移植進 `src/AprVisual`。骨架已建好（commit `06c8d29`），這份清單追蹤把每個 stub 填實的進度。

- 參考實作：`ref/metalnes-main/source/metalnes/`（主要是 `wire_module.cpp` 2340 行、`wire_defs.cpp`、`wire_node_resolver.cpp`、`system.cpp`、`handler_*.h`）
- 參考資料：`ref/metalnes-main/data/system-def/*.js`（測試用的真實 `.js` 模組定義）
- 對照筆記：`MD/note/`（01 演算法 / 02 模組系統 / 03 系統整合 / 04 驗證）
- 設計決議：`MD/struct/08`（四階段路線圖）、`MD/struct/09`（S1 實作風格）

狀態圖示：⬜ 未開始 / 🟦 進行中 / ✅ 完成

---

## Step 1 — `WireCore.Parse.cs`：`.js` 模組格式載入器 ✅

對應 `wire_defs.cpp`（`wire_defs::Load` / `loadJsonFile` / `loadExternalFiles` / `deserialize<...>` 系列）。實作：`Sim/JsModuleFormat.cs`（`JsLexer` + `JsReader`）+ `Sim/WireCore.Parse.cs`（`LoadModuleDef` + entry parsers）+ `Test/TestRunner.cs`（`--dump-module` / `--system-def-dir`）。

- ✅ **1a** JS-ish lexer（`JsModuleFormat.cs` `JsLexer`）：跳 whitespace、**逗號當空白**（結構靠 `{}[]:=` 即可，省掉所有 comma 處理）、`//` 行註解、`/* */` 區塊註解、行號追蹤；token = `{ } [ ] : =`、字串（單/雙引號）、數字（含負號 + 容忍 `.0`）、識別字
- ✅ **1b** reader（`JsReader`）：`ExpectVarHeader` / `ReadObject(onKey)` / `ReadArray(onElement)` / `BeginArray`+`AtArrayEnd`+`EndArray`（fixed-shape array 用，`EndArray` 自動跳過剩餘元素）/ `ReadString`（容忍 bare ident）/ `ReadInt` / `ReadBool` / `TryReadBool`（optional 的 IsWeak 用）/ `PeekKind` / `PeekText` / `SkipValue`
- ✅ **1c** `LoadModuleDef(dir, name)`：吃 `var module =`，`ReadObject` 按 key 分派填 `ModuleDef`（全部欄位）；遞迴載入 `modules` 子模組；**cache 於 `_loadedDefs`（keyed by name）** 讓重複型別（2× `SRAM2K`、2× `74LS368`、2× `nes-pad`）只 parse 一次（Step 2 用 `LoadedDefs` 查）
- ✅ **1d** 外部檔載入（`LoadExternalArray`）：`nodenames_files`/`transdefs_files`/`segdefs_files` → 相對於模組目錄載入 `var nodenames = {...}` / `var transdefs = [...]` / `var segdefs = [...]`，併進同一個 `ModuleDef`
- ✅ **1e** `transdef` 解析（`ReadTransDef`）：`[name, gate, c1, c2]` 後若是 `[` 跳一個陣列（bbox）、若再是 `[` 跳一個陣列（geom）、若是 `true`/`false` 讀成 `IsWeak`
- ✅ **1f** `segdef` 解析（`ReadSegDef`）：`[node, pull, layer, x1,y1,...]` — 取 `node` 與 `pull`（`'+'`/`'-'`/無），`EndArray` 跳掉 polygon 座標
- ✅ **1g** `noderef` 解析（`ReadNodeRef`）：數字 → `NodeRef(int)`；字串 → `NodeRef(string)`
- ✅ **1h** 容錯：trailing comma（逗號當空白）、`var nodenames ={`、引號/bare key、`/` `#` `~` `_` 前綴名字（出現在引號 key 裡）、未知 key（`SkipValue`）
- ✅ **驗收**：`--dump-module` 對 `74HC04`（6 trans / 6 seg `+` / 14 pin / 0 sub）、`SRAM2K`（9 trans / 1 seg / 24 pin / memory `ram(2048)`）、`2a03`（1953 named node / 27711 segdef [+:14493 −:13218] / 10946 transdef / 40 pin / 外部檔 3 個 / 內部暫存器名 `a0 x0 y0 pcl0` 都在）、`cart-mmu0`（3 conn / 1 sub `edge→nes-cart72`）、`nes-001`（33 node / 82 conn / 13 sub，遞迴載完 13 種 def 含 `4021`/`pslatch`；2C02 = 7130 node / 16806 trans / 35616 seg）全部解析無誤；`dotnet build` 0 警告 0 錯誤
- ⏳ **尚未**：`--rom` 仍丟「parse not implemented」之後的 stub（`AddInstance`/`Reset`/`StepCycle` 還沒實作）— 那是 Step 2+

## Step 2 — `WireCore.Module.cs`：模組組裝 ✅

對應 `wire_module.cpp`（`Wires::addInstance` / `setupNodes`/`setupPins`/`setupMemory`/`setupSegments`/`setupTransistors` / `addConnection` / `addNode` / `addTransistor`）+ `wire_node_resolver.cpp`。實作：`Sim/WireCore.Module.cs`（組裝邏輯 + `Node` build-time class）+ `Sim/WireCore.System.cs`（`ComposeSystem`）+ `Test/TestRunner.cs`（`--dump-system`）。

- ✅ **2a** `AddInstance(def, prefix)`：新 prefix → `AllocNodes(alignment, maxLocalId+1)`（`alignment = maxNode<100 ? 100 : 1000`）切一段；本地 id `Remap`（`vss`→`Ngnd`、`vcc`→`Npwr`、其餘 `nodeStart+id`）；`setupNodes`（每個 nodename：`AddNode(Remap(id), prefix.name)`）+ `setupPins`（加 `#<pin>` alias）+ `setupMemory`（建 `Memory`）+ `setupSegments`（pull-up 計數）+ `setupTransistors`；**已 setup 的 prefix 會重用**（只重跑 sub-module 遞迴 / connections / forceCompute）— 卡匣的 prgrom/chrrom/extraram 用 prefix `""`（→ instantiated under `"cart"`，重用 cart 實例）就靠這個
- ✅ **2b** `AddTransistor(name, gate, c1, c2, isWeak)`：`(gate==c1==c2 == EmptyNode)` 跳過；`c1==c2` 跳過；`IsPwrGnd(c1)` → swap 讓 supply 在 c2；**dedup by `(gate,c1,c2)`**（`HashSet`）；append `List<Transistor>`；登記 `Node.Gates` / `Node.C1c2s`
- ✅ **2c** segdef `'+'` → `Node.Pullups++`；`pullups:[]` 也算（`ResolveNodes` 展開後 `++`）。註：MetalNES 的 2A03/2C02 資料裡**所有** transistor 的第 7 欄都是 `false`（weak: 0），所以 `IsWeak` 目前不影響任何東西 — plumbing 留著
- ✅ **2d** `AddConnection(from, to)` = `AddTransistor("from<>to", gate:Npwr, c1:from, c2:to)`（永遠導通）；`AddConnection(fromExpr, toExpr)` 用 `ResolveNodes` 展開，one-to-many（左邊 1 個）或 one-to-one（兩邊等長）
- ✅ **2e** `ResolveNodes(expr, outIds)`：`"x|y|z"` split；`"*foo"` wildcard（掃 `_nodeByName`，前綴/後綴比對）；`"a[7:0]"` → `[a0,a1,...,a7]`（注意 index 順序對應 bit 順序，照 MetalNES，**不是**大端）；`"a[]"` → `[a0,a1,...]` 到查不到；單一名字（缺 → `Ngnd`）
- ✅ **2f** `LookupNode` / `GetNodeName`（含 `vcc`/`vss` fallback）/ `AllocNodes` / `AddNamedNode`（callback target 用）；`_nodeByName`（name→id，唯一）/ `_nameByNode`（id→name，first wins，alias 也都進 `_nodeByName` 給 wildcard 用）/ `_maxNodeId`；`ResetBuild()` 清全部 build state + 重註冊 `vcc`/`vss`；`MaxNodeIdOf`、`CombinePrefix`、`GetOrCreateNode`
- ✅ **驗收**：`--dump-system`（compose `nes-001` + `cart-mmu0` + prgrom/chrrom/extraram）→ global node array 41218 / 非空 node 15164 / pull-up 6398 / transistors 27305（含 527 connection-transistors，dedup 後）/ forceCompute 8（`ppu.spr_d[7:0]`）/ memories `u1.ram` `u4.ram` `cart.prg.rom` `cart.chr.rom` `cart.eram.ram`；`LookupNode` 對 `clk`/`res`/`cpu.clk0`/`cpu.a0`(=13737, 內部累加器!)/`cpu.ir0`/`ppu.clk0`/`u1.cs`/`u3.1/Y1`/`cart.prg.a0` 全部回合理 id；`ResolveNodes` 對 `cpu.ab[15:0]`(16)/`cpu.db[7:0]`(8)/`cpu.a[7:0]`(8)/`cart.edge.cpu_a[14:0]`(15) 正確，`*.vss`/`*.vcc` 全部 fold 到 `Ngnd`(2)/`Npwr`(1)；**整個 compose stderr 0 行**（無 connection-failed / unknown-node / missing-pin warning）；`dotnet build` 0/0

## Step 3 — `WireCore.cs Reset()`：建攤平陣列 + LUT ✅

對應 `wire_module.cpp` `wire_compute::reset()`（~L1400-1517）。實作於 `Sim/WireCore.cs`（取代原 stub）。

- ✅ **3a** alloc unmanaged 陣列（`AllocArray<T>`，64-byte 對齊，tracked）：`NodeStates[NodeCount]`、`NodeInfos[NodeCount]`、`RecalcList`/`RecalcListNext`/`RecalcHash`/`RecalcHashNext`/`_groupBuf`（各 `NodeCount`）、`FlagsToState[256]`。`Reset()` 開頭先 `FreeUnmanagedMemory()` → re-runnable
- ✅ **3b** `BuildFlagsToStateTable()`（在 `FlagsToState` alloc 後呼叫）
- ✅ **3c** 每個非空 node：`NodeInfos[n].Connections = C1c2s.Count + Gates.Count`；`Flags = None`（= setFloat）；`Pullups > 0` → `|= PullUp`、`NodeStates[n] = 1`；有 `Callback` → `|= HasCallback`。null node 留零（`AllocArray` 已清零）
- ✅ **3d** 建 `TransistorList`：index 0 是 sentinel 0；每個非空 node：`TlistGates` = 攤平 `Gates` 的 `(c1,c2,...)` + 結尾 0；`C1c2s` 走訪、`t.Gate == Ngnd` 的跳過、`other = (t.C1==nn ? t.C2 : t.C1)`，依 `other` 是 Ngnd / Npwr / 普通 分到 `TlistC1gnd`(只存 gate) / `TlistC1pwr`(只存 gate) / `TlistC1c2s`(存 gate,other)，各 + 結尾 0。空 sub-list → index 0（指向 sentinel，`while(*p)` 立即停）。最後 copy `List<int>` → unmanaged `TransistorList`（長度 `TransistorListLength`）
- ✅ **3e** `NodeStates[Ngnd]=0; NodeInfos[Ngnd].Flags=Gnd; NodeStates[Npwr]=1; NodeInfos[Npwr].Flags=Pwr`（在 loop 之後覆寫；ngnd/npwr 的 tlist 也會被建但無害，因為 group BFS 從不遞迴進它們）
- ✅ **3f** 套用 `ForceComputeList` → `|= ForceCompute`；`Time = 0`、`RecalcListCount = RecalcListNextCount = 0`
- ✅ **驗收**：`--dump-system` 末段 → `Reset()` 跑完：`NodeCount` 41218、`TransistorList` 長度 182027、state=1 的 node 數 6399（= 6398 pull-up + 1 vcc，正確）、forceCompute 8、`cpu.clk0` build-time c1c2s 7 個；`dotnet build` 0/0；stderr 0 行

## Step 4 — `WireCore.Group.cs`：node group BFS ✅

對應 `wire_module.cpp` `addNodeToGroup` / `computeNodeGroup` / `getNodeValue`（~L1583-2030）。`getNodeValue` 與 `FlagsToState` 已實作；本步補 `AddNodeToGroup`（遞迴，照 MetalNES）。

- ✅ **4a** `AddNodeToGroup(nn)`：`_inGroup[nn]` flag 做 **O(1) dedup**（MetalNES 用 `_groupBuf` 線性掃 — 同效果，這裡用 flag 陣列，`ComputeNodeGroup` 開頭清掉前一個 group 的 flags）；push；`Connections > _maxConnections` → 記 `_maxState`/`_maxConnections`；`RecalcHash[nn]=0`（加入 group → 本 pass 不用單獨重算）；`_groupFlags |= ns.Flags`；走 `TlistC1c2s`（gate,other 對，gate on → 遞迴 `AddNodeToGroup(other)`）；走 `TlistC1gnd`（gate on → `_groupFlags |= Gnd`，break）；走 `TlistC1pwr`（同理 `|= Pwr`）
- ⏳ **4b** 回傳 `NodeValue.HoldPrevious` 的變體 — 暫緩（`NodeValue` enum 已宣告；S1 先用 `_maxState`）
- ✅ **驗收**：見 Step 5（手刻電路 + `--selftest`）

## Step 5 — `WireCore.Recalc.cs`：傳播迴圈 + step ✅

對應 `wire_module.cpp` `recalcNodeList` / `processQueue` / `recalcNode` / `setNodeState` / `enqueueNode` / `step_cycle`（~L1519-1928, L730-773）。整檔重寫（取代 stub）。

- ✅ **5a** `EnqueueNode(nn)`：`if RecalcHashNext[nn]==0 { RecalcListNext[RecalcListNextCount++]=nn; RecalcHashNext[nn]=1 }`（npwr/ngnd 跳過）
- ✅ **5b** `SetNodeState(nn, newState)`：`if NodeStates[nn]!=newState { NodeStates[nn]=newState; 走 TlistGates: c1=*p++,c2=*p++; EnqueueNode(c1); if newState==0 && c2!=Npwr && c2!=Ngnd EnqueueNode(c2) }`
- ✅ **5c** `RecalcNode(nn)`：`newState = ComputeNodeGroup(nn)`；`for gnn in _groupBuf[0.._groupCount): SetNodeState(gnn, newState)`；`_groupFlags & HasCallback` → group 內有 callback 的 node `EnqueueCallback`
- ✅ **5d** `ProcessQueue()`：`while RecalcListNextCount!=0 { iteration++; if==100 stderr warning; 用 temp 變數 swap List/ListNext + Hash/HashNext + counts; for nn in RecalcList: if RecalcHash[nn]!=0 { RecalcNode(nn); RecalcHash[nn]=0 } RecalcListCount=0 } InvokeCallbacks()`（pointer 不能 tuple-swap，改用 `int* tmp`）
- ✅ **5e** `RecalcNodeList(nn)` / `RecalcNodeList(span)`；`RecomputeAllNodes()`（power-on：enqueue 所有非 supply node 再 ProcessQueue）
- ✅ **5f** `SetHigh/SetLow/SetFloat(nn)` 與 `(string name)` 版；`IsNodeHigh(int/string)`；`GetNodeFlags`
- ✅ **5g** `StepCycle()`：toggle `ClockNode`；`RunHandlerChain()`；`if TraceLevel!=0 CaptureTraceLine`；`Time++`。`Step(count)` loop
- ✅ **驗收**：`--selftest`（手刻 inverter / NAND / pass-transistor + dynamic-hold，照 `MD/struct/04 §13.3` / `chip_tests.cpp` 的 pslatch/4021）→ **13/13 全 PASS**：inverter `y=!a` 四態；NAND `y=!(a&b)` 真值表 4 組；pass transistor en=1 時 out 跟著 in、en=0 時 out 保持前值（dynamic hold）、再 en=1 又跟著 in；`dotnet build` 0/0

## Step 6 — `WireCore.Handlers.cs`：handler + callback + 行為級記憶體 ✅（video 為 Step 7 placeholder）

對應 `handler_clock.h` / `handler_ram.h` / `handler_rom.h` / `handler_video_out.h` + `Wires::add_callback` / `invoke_callbacks`。整檔重寫（取代 stub）。

- ✅ **6a** `AddCallback(watchedNodes, cb)`：`AddNamedNode` 配一個假 target node；對每個 watched node 加 `AddTransistor(gate:watched, c1:target, c2:Ngnd)`；target node 設 `Callback = CallbackInfo`（→ Reset 時自動標 `HasCallback`）。**必須在 `Reset()` 之前呼叫**（會新增 node/transistor）
- ✅ **6b** `EnqueueCallback`（設 `Enqueued`）/ `InvokeCallbacks`（在 `ProcessQueue` 末端；fast-path 檢查；re-entrant-safe — callback 內可再驅動 node，`Enqueued` 旗標防重複入列）；`ResetHandlers()`（清 `_handlerChain` / `_callbacks`，被 `ResetBuild` 呼叫）
- ✅ **6c** `AttachClockHandler()`：`AddHandler(() => toggle "clk" 節點)`。`StepCycle` 不再自己 toggle ClockNode（避免雙重 toggle），改由 clock handler 負責
- ✅ **6d** `AttachMemoryHandlers()`：`ResolveNodes("*func<ram>")` / `("*func<rom>")` 找掛鉤點 → `PrefixOf` 取模組 prefix → `AttachRamLikeHandler`：解析 `cs` / `/we` / `a[]` / `_d[7:0]`（handler 驅動的內部資料線）/ `d[]`（外部資料腳，watch 用）；`AddCallback({cs,/we,a[],d[]}, () => { if cs 非低 return; addr=ReadBits(a); writing = !rom && /we==0; writing ? mem.Write(addr, ReadBits(_d)) : WriteBits(_d, mem.Read(addr)); })`。`WriteBits` = 逐 bit `SetHigh`/`SetLow`（持久驅動，跟 MetalNES 一樣 — 晶片 deselect 時內部 pass transistor 自然斷開 bus）；`ReadBits(IReadOnlyList<int>)`
- ⏳ **6e** `AttachVideoHandler()`：目前只 alloc 黑色 `FrameBuffer`（placeholder）。TODO（Step 7）：watch PPU `vid_[11:0]` + pclk，把 composite 電壓階梯解碼成 RGB 寫進 `FrameBuffer[y*256+x]`。audio S1 整個跳過
- ✅ **驗收**：`--selftest` 加 callback 測試（假電晶體 watch：node 變 → callback fire；node 不變 → 不 fire）→ 全套 **16/16 PASS**；`dotnet build` 0/0。（RAM handler 透過真實 NES ROM 的端到端驗證留到 Step 7）

## Step 7 — `WireCore.System.cs` + `WireCore.Trace.cs`：整機 + 驗證鉤子 ✅（CPU 跑起來了！收斂 bug 已修；video output 與全 test-ROM 跑通是後續）

對應 `system.cpp` `system_state::Create` / `setupRom` + `handler_nes_system.h` `reset()` / `check_unit_test()`。

- ✅ **7a** `LoadSystem(rom)`：`ComposeSystem(chrIsRam, isTestRom)`（內含 `LoadModuleDef("nes-001")` + `cart-mmu0` + 附加 prgrom/chrrom(或 chrram)/extraram 子 instance + `AddInstance`）；`CopyRomBytes`（PRG 16KB → mirror 兩份 / 否則 copy；CHR → `cart.chr.rom`）；`AttachClockHandler` / `AttachMemoryHandlers` / `AttachVideoHandler`（**在 Reset 之前** — handler 會加 callback node/transistor）；`ResolveCachedNodes`（`ClockNode`/`N_Res`/`N_PpuInVblank`/`N_CpuSync`/`R_CpuA..R_CpuDb`/`M_EramRam`，用 `ResolveNodes(..., quiet:true)`）；`ResetNes(full:true)`
- ✅ **7b** `ResetNes(bool full)`：full → 清 `u1.ram`/`u4.ram`/`cart.chrram.ram` + `Reset()` + `FrameBuffer = AllocArray<uint>(256*240)`；皆 → `SetHigh(N_Res); Step(12*8*2=192); SetLow(N_Res)`。`SoftReset()` = `ResetNes(false)`
- ✅ **7c** `RunFrame(maxHalfCycles)`：watch `ppu.in_vblank` 上升沿就停；無此 node 則 step 固定量。回傳跑了幾 half-cycle
- ✅ **7d** `WireCore.Trace.cs`：`SetTraceColumns(csv)`（每個 expr 用 `ResolveNodes` quiet 解析）/`CaptureTraceLine`（逗號分隔欄位 + `Time` + hex）/`DumpCpuState`（讀 `R_CpuA..R_CpuPch`/`R_CpuAb`/`R_CpuDb`/`N_CpuSync` → `PC=.. A=.. X=.. ...`）/`CheckUnitTest`（讀 `M_EramRam.Data`：`[1..3]==0xDE 0xB0 0x61` 簽章、`[0]` 結果碼、`[4..]` NUL 結尾文字）。`ProcessQueue` 加 hard cap 1000 passes（100 時 warning）
- ✅ **7e** `TestRunner`：`--trace <rom> [--cycles N]`（power-on reset → step N 個 6502 cycle、每 cycle dump CPU state）；`--test`/`--test-dir` 改成真的 `LoadSystem` + loop `RunFrame` + `CheckUnitTest`；`MainForm`（`--rom`）改成每 timer tick `Step(50000)` + 標題列顯示 `DumpCpuState`（video 仍是 placeholder 黑畫面）；`AttachVideoHandler` 暫為 no-op
- ✅ **7f**（收斂 bug 修正）：`ResetNes(full:true)` 在 `Reset()` 之後**加 `RecomputeAllNodes()`**（= MetalNES `Wires::resetState()` 的 `_compute->reset()` + `recomputeAllNodes()`）。之前缺這個 → `Reset()` 後網表停在「pull-up→1、其餘→0」的 raw 狀態（非合法 quiescent），`SetHigh(res)` 從那裡傳播就 thrash。加上之後 recalc **收斂**（stderr 0 行，無 cap abort）
- ✅ **驗收**：`--trace branch_timing/1.Branch_Basics.nes --cycles 16` → reset 序列 7 cycle → 讀 reset vector `$FFFC/$FFFD` = `$E124` → `PC=E124` 取指 `$20`(JSR) → push 返回位址到 stack → `PC=E0BB` 取指 `$78`(SEI) → `$A9`(LDA #) —— **完全是教科書 6502 reset 後的 NES init 序列，CPU 跑對了**；`--rom <nrom.nes>` 開視窗、跑 12s 不 crash（標題列顯示 live CPU state）；`--selftest` 16/16；`dotnet build` 0/0
- ✅ **後續 (a) video handler — 真實 palette 已做**：`AttachVideoHandler`：`ppu.pclk1` 上升沿 → 可見區（hpos<256, vpos<240）內 `slot = pal_ptr[4:0] & 31`、`colour6 = ppu.pal_ram_<slot>_b[5:0]`（= MetalNES `handler_palette_ram` 讀的那個值，palette RAM 6T cell 的 `b` 側）、`FrameBuffer[y*256+x] = NesPalette[colour6 & 0x3F]`（硬編 64 色「2C02 NTSC」master palette）。**不是 pixel-perfect**：hpos→column 可能差 ~1（pipeline latency 沒修）、用近似 RGB master palette（真實要走 `vid_[11:0]` composite ladder + NTSC 解調，但 S1 這樣夠了）、忽略 colour emphasis（`vid_emph`）。node 不解析 → 黑畫面不 crash。遊戲/test ROM init 寫好 palette 之後，`--rom` 應該會顯示真實畫面
- 🟦 **剩下的後續（非 S1 阻擋項）**：
  - (b) 全 test-ROM 跑通（`--test` 能跑但慢，test ROM 要好幾個 frame；S3 優化）
  - (c) trace 與 MetalNES/Perfect6502 逐 byte 並排比對 ← `MD/struct/08` 的 S1 正式過關條件（CPU trace 目視看是對的）
  - ✅ (d) **~~`$2002` 讀取期間 D7 = 0 的 bug~~ → 不是 bug；`full_palette.nes` 已用 `--screenshot --frames 48` 跑通(畫面 = 64 色 palette 格子 + emphasis 染色，見 `etc/fp_48.png`)**：
    - 把 `--probe-vbl` 延長成 160 half-cycle、抓「`vbl_flag` 還是 1 的時候那次 `$2002` 讀取」（= `wait_vbl` 的 `bit $2002`，vpos 241 dot ~13）逐 half-cycle trace，結果：data half-cycle 那一拍 `read_2002_output_vblank_flag=1`、`vbl_flag` 在同一拍被清成 0、但 `/vbl_flag_read_buffer` 仍 hold 0（`t6661` 已關）、`vbl_flag_read_buffer_out=1`、`_io_db7=1`、**`io_db=0x80`** —— **D7 確實是 1（清前值），read-buffer 抓對了**。之前 v2 看到的「D7=0」是因為當時 trace 的是 vpos 240 那次讀取（flag 本來就還沒 set，讀到 0 是對的），不是 flag 升起後的讀取。
    - 那 `full_palette.nes` 跑 8 frame 還是純 backdrop、CPU 卡 PC=$8053 是怎麼回事？看 `full_palette.s`：reset 後做 `jsr wait_vbl` ×2（這兩次都讀到 0x80、正常返回）、寫 `$2000/$2001=0`、清 palette / nametable，然後是一段**故意的「精準同步到 VBL」迴圈**：
      ```
      jsr wait_vbl
      nop
    : nop
      lda $2002
      lda $2002      ; ← BPL 測這次的 bit7
      pha / pla / pha / pla
      bpl :-          ; bit7==0 就 loop（迴圈共 27 CPU cycle）
      ```
      VBL 週期 ≈ 29780.67 CPU cycle、迴圈 27 cycle，所以讀取點每 frame 相對 VBL 漂 ~1 PPU dot；要等到「第一次 `lda $2002` 落在 VBL set 之前、第二次落在 VBL set 之後（≈12 dot 的窗）」迴圈才會 exit。讀取每 ~81 dot 一次 → 最壞 ~81 frame、平均 ~40 frame 才收斂。**8 frame 根本不夠**。PC=$8053 就是這個迴圈裡的 `lda $2002`。
    - **驗證結果**（`--screenshot ... --frames 48` Release）：每 frame = `714736` half-cycle（= 89342 PPU dot = 357368 master clock，長度正確）；同步迴圈在 **frame ~10-17 收斂**（CPU 脫離 `PC=$805x`，跳到 `$8163/$8168` 的 `begin_frame`/`draw_row`/`triplet` 區、`A=30 X=3D Y=0A`）；之後 frame 長度在 `714736`/`714728` 交替（差 8 half-cycle = 1 PPU dot = `sta $2001` 開 BG 後「奇數 frame 少一 dot」，跟真機一致）；frame 48 截圖 = 完整 64 色 palette 格子 + emphasis。**邊緣有橫向拖影/位移**（hpos→column 差 ~1 的 pipeline latency；近似 RGB master palette、沒做 composite ladder/NTSC 解調）—— 留給 (e) video 精修。
    - （這條從「subtle timing bug」降級成「test ROM 本來就慢、要多跑 frame」並已驗證通過；真正待辦回到 (b) 全 test-ROM 跑通 = S3 的吞吐量工作）
  - (e) video 精修（composite ladder + NTSC 解調 / 精確座標 / emphasis）
- 🛠️ **驗證 / 診斷工具**：`--screenshot <rom> [--frames N] [--out p.png]`（FrameBuffer → PNG）；`--ppu-dump <rom> [--frames N]`（palette RAM / VRAM / rendering 狀態 / pal_ptr）；`--benchmark <rom> [--frames N]`（headless 吞吐量：模擬 FPS / MIPS（6502 cyc/s）/ raw step 率）；`--probe2002 <rom>`（一次 `$2002` 讀取的 CPU↔PPU register path）；`--probe-vbl <rom>`（trace 2C02 的 `vbl_flag` 等，change-detection）；`--dump-node <name>`（列一個 node 的 pull-up + 它 gate 的 transistors + 它當 channel end 的 transistors）
- 📊 **效能基準（S1 switch-level interpreter，S3 的「before」基線）**：Release build、`full_palette.nes` 24 frame：**FPS ≈ 0.06**（≈ 0.1% realtime；1 秒 NES ≈ 15.5 分鐘模擬）、**≈ 1,900 模擬 6502 cycle/s（≈ 0.0019 MHz，0.1% of 1.79 MHz 2A03）**、底層 inner loop ≈ **46k switch-level step（master half-cycle）/s**（41k node / 27k transistor 的網表、每 half-cycle settle 到 fixed point）。compose + power-on settle ≈ 0.36 s。← S3 的 IR 解譯器要明顯打爆這個數字才算過關（`MD/struct/08`）；Debug 跟 Release 差不多（pointer-heavy，最佳化幫助有限）

---

## 進度日誌

| 日期 | commit | 完成 | 備註 |
|------|--------|------|------|
| 2026-05-12 | `06c8d29` | 骨架（19 檔，build 通過） | `WireCore.*` 全 stub；`FlagsToState` LUT、iNES parser、GDI rendering、CLI 已實作 |
| 2026-05-12 | `2997f98` | **Step 1**：`.js` 模組格式載入器 | 新增 `Sim/JsModuleFormat.cs`（lexer+reader）；實作 `WireCore.Parse.cs` 的 `LoadModuleDef` + entry parsers + `_loadedDefs` cache；`TestRunner` 加 `--dump-module` / `--system-def-dir`。驗收：74HC04 / SRAM2K / 2a03 / cart-mmu0 / nes-001 全部 dump 無誤（含外部檔 + 13 層遞迴），build 0/0 |
| 2026-05-12 | `5f8da18` | **Step 2**：模組組裝 | 實作 `WireCore.Module.cs`（`AddInstance`/`AddNode`/`AddTransistor`(dedup)/`AddConnection`(=永遠導通電晶體)/`ResolveNodes`(`\|`/`*`/`[hi:lo]`/`[]`/single)/`GetOrCreateNode`/`Node` class/`ResetBuild`）+ `WireCore.System.cs` 的 `ComposeSystem`（nes-001 + cart-mmu0 + prgrom/chrrom/extraram）+ `TestRunner --dump-system`。驗收：compose 整台 NES → 15164 非空 node / 27305 transistor（527 connection）/ 5 memory / forceCompute 8；所有 lookup/resolve 探針正確；compose stderr 0 行；build 0/0 |
| 2026-05-12 | `8333d54` | **Step 3**：`Reset()` 建攤平陣列 + LUT | 實作 `WireCore.cs Reset()`（取代 stub）：alloc unmanaged hot 陣列、`BuildFlagsToStateTable`、per-node 初始化（pull-up→state 1）、建攤平 `TransistorList`（3-way bucket，0-terminated）、設 Ngnd/Npwr flags、套 forceCompute。`--dump-system` 末段加 Reset 統計。驗收：NodeCount 41218 / TransistorList 182027 / state=1 共 6399（6398 pull-up + vcc）/ forceCompute 8；build 0/0 |
| 2026-05-12 | `6b1a96b` | **Step 4+5**：group BFS + recalc 迴圈 + step | `WireCore.Group.cs` 補 `AddNodeToGroup`（遞迴）；`WireCore.Recalc.cs` 整檔重寫（`EnqueueNode`/`SetNodeState`/`RecalcNode`/`ProcessQueue`(雙緩衝 swap + hash 去重 + 100-iter warning)/`RecalcNodeList`/`RecomputeAllNodes`/`SetHigh`/`SetLow`/`SetFloat`/`IsNodeHigh`/`StepCycle`/`Step`）。`TestRunner --selftest` 加手刻 inverter/NAND/pass-transistor+dynamic-hold。驗收：`--selftest` **13/13 PASS**（含 dynamic hold：en=0 時 out 保持前值）；build 0/0 |
| 2026-05-12 | `11ffd7c` | **Step 6**：handler + callback + 行為級記憶體 | `WireCore.Handlers.cs` 整檔重寫：`AddCallback`(=假電晶體 watch)/`InvokeCallbacks`(re-entrant-safe)/`ResetHandlers`/`AttachClockHandler`(toggle "clk")/`AttachMemoryHandlers`(`*func<ram>`/`*func<rom>` → RAM/ROM 讀寫回驅 `_d[]`)/`WriteBits`/`ReadBits`/`PrefixOf`；`AttachVideoHandler` 暫為黑畫面 placeholder。`StepCycle` 移除 ClockNode toggle（改由 clock handler）。`--selftest` 加 callback 測試。驗收：`--selftest` **16/16 PASS**；build 0/0 |
| 2026-05-12 | `4b0b067` | **Step 7**（鷹架）：整機 + 驗證鉤子 | `WireCore.System.cs` 重寫：`ComposeSystem`/`LoadSystem`/`ResetNes(full)`/`SoftReset`/`RunFrame`。`WireCore.Trace.cs` 重寫：`DumpCpuState`/`CheckUnitTest`($6000)/`SetTraceColumns`/`CaptureTraceLine`。`ProcessQueue` 加 1000-pass hard cap。`TestRunner` 加 `--trace`；`--test` 真的跑；`MainForm` 每 tick `Step(50000)` + 標題列。`ResolveNodes` quiet 旗標。**當時已知問題**：recalc 不收斂、CPU 卡 reset（下一行修掉了）|
| 2026-05-12 | `284f622` | **Step 7 完成**：修收斂 bug — CPU 跑起來了 | `ResetNes(full:true)` 在 `Reset()` 後加 `RecomputeAllNodes()`（= MetalNES `resetState()` 的 `recomputeAllNodes()`，之前漏了）。修好後 recalc 收斂（stderr 0）。`--trace branch_timing/1.Branch_Basics.nes`：reset → 讀 reset vector `$E124` → `JSR $E0BB` → `SEI` → `LDA #` —— 教科書 6502 NES init 序列，**CPU 跑對了**。`--rom` 開視窗跑 12s 不 crash。`--selftest` 16/16。build 0/0。**S1 核心功能達成**（video output / 全 test-ROM 跑通 / 與 MetalNES 並排逐 byte 比對為後續）|
| 2026-05-12 | `bf4ecc7` | 優化：group dedup O(n²)→O(1) | `WireCore.Group.cs` 的 `AddNodeToGroup` 把「線性掃 `_groupBuf`」改成「`_inGroup` flag 陣列」（MetalNES 用線性掃；這裡 O(1)）。`ComputeNodeGroup` 開頭用前一個 `_groupBuf`/`_groupCount` 清掉 flags（保持 `_inGroup` 兩次呼叫間全零）。行為不變（`--selftest` 16/16、`--trace` 輸出一致），但大 group（power-on settle）快很多。build 0/0 |
| 2026-05-12 | `c0814b6` | video handler — first cut | `AttachVideoHandler`：`AddCallback` 在 `ppu.pclk1` 上升沿 → 讀 `ppu.hpos[8:0]`/`vpos[8:0]`/`pal_ptr[4:0]`，可見區內把 `FrameBuffer[y*256+x] = approxPalette[pal_ptr&31]`（32 色近似 ramp，非真實 palette）。下一行升級成真實 palette |
| 2026-05-12 | `697012b` | video handler — 真實 NES palette | `AttachVideoHandler` v2：可見區內 `slot = pal_ptr & 31`、`colour6 = ppu.pal_ram_<slot>_b[5:0]`（= MetalNES `handler_palette_ram` 讀的值）、`FrameBuffer = NesPalette[colour6 & 0x3F]`（硬編 64 色 2C02 NTSC master palette）。驗證：`--rom <nrom>.nes` 跑 12s 不 crash、`--selftest` 16/16、`--trace` 輸出一致、stderr 0（32 個 `pal_ram_XX_b[5:0]` register list 都解析到）。build 0/0 |
| 2026-05-12 | `7b53e65` | `--screenshot` + 目視驗證 | `TestRunner --screenshot <rom> [--frames N] [--out p.png]`：跑 N frame → 把 FrameBuffer 包成 `Bitmap` 存 PNG。跑 `full_palette.nes --frames 6` → 純黑（= palette[0] 0x0F，backdrop 正確），但 PPU 前景沒畫 → 發現 PPU rendering bug |
| 2026-05-12 | `c58f4f2` | `--ppu-dump` 診斷 → 定位 bug | `TestRunner --ppu-dump`：跑 N frame 後 dump palette RAM / VRAM / `rendering_disabled`/`in_vblank` / pal_ptr 取樣。`full_palette.nes --frames 4` → CPU 卡在 `LDA $2002` 一直 poll vblank flag 讀不到 → 永遠不 enable rendering → 螢幕停在 backdrop。bug 在 PPU 暫存器路徑 |
| 2026-05-12 | `f3b1423` | `--probe2002` → 把 bug 縮到 PPUSTATUS read | `--probe2002 <rom>`：一次 `$2002` 讀取的 CPU↔PPU register path。位址解碼/data bus OK，但 PPU 驅動 `io_db=$00`（bit7≠vbl flag）即使 `in_vblank=1` → bug 在 2C02 內部 |
| 2026-05-12 | `1ad5944` | `--probe-vbl` → `vbl_flag` 待不住 | trace `set_vbl_flag`/`vbl_flag` 等。`set_vbl_flag` 正確在 vpos 241 dot 1 pulse、`vbl_flag`→1，但幾個 dot 後又被清回 0 |
| 2026-05-12 | `785ffbb` | `--probe-vbl` v2 → 清 flag 的就是 CPU 的 `$2002` 讀取，但 D7 拿到清後的 0 | 每個 frame `vbl_flag`→0 那一步 `cpu.ab=$2002, /r2002=0, io_ce=0` —— 是讀 `$2002` 清的（對的），但我們 settle 內 flag 同步清 → data phase `io_db=$00` → CPU latch D7=0 → stuck |
| 2026-05-12 | `163e6a2` | `--dump-node` → 挖出 `vbl_flag` read-buffer 結構 | `TestRunner --dump-node <name>`：列 node 的 pull-up + 它 gate 的 transistors + 它當 channel end 的 transistors。挖 `ppu.vbl_flag`/`/vbl_flag`/`read_2002_output_vblank_flag`/`/r2002`/`vbl_flag_read_buffer_out`/`/vbl_flag_read_buffer`/`vbl_clear_flags`：`vbl_flag` = 靜態 cross-coupled latch；clear via `t7085` gate=`read_2002_output_vblank_flag`(=「`$2002` 讀取中」)；D7 ← read-buffer `vbl_flag_read_buffer_out` ← dynamic node `/vbl_flag_read_buffer`（`t6661` gate=`/read_2002_output_vblank_flag`「沒讀時」擷取 `/vbl_flag_out`、「讀時」hold）。當時懷疑 = read-buffer 擷取時序 bug。詳細結構記在 §7f(d)。`--selftest` 16/16、build 0/0 |
| 2026-05-12 | `61741ab` | **釐清「`$2002` D7 bug」其實不是 bug** | `--probe-vbl` 延長成 160 half-cycle、抓「`vbl_flag`=1 時那次 `bit $2002`」（vpos 241 dot ~13）逐 half-cycle：data 拍 `read_2002=1`、`vbl_flag`同拍→0、但 `/vbl_flag_read_buffer` hold 0、`vbl_flag_read_buffer_out=1`、**`io_db=0x80`** → **D7 = 1（清前值），read-buffer 抓對了**。之前 v2 的「D7=0」是 trace 到 vpos 240（flag 本來還沒 set）的讀取。`full_palette` 卡 PC=$8053 = `full_palette.s` 那段故意的 27-cycle VBL 同步迴圈，本來就要 ~10-80 frame 收斂，8 frame 不夠。§7f(d) 從「timing bug」降級成「test ROM 慢、要多跑 frame」。`PpuDump` 加每 10 frame 進度行（`DumpCpuState`+`rendering_disabled`+`pal_ram!=0F`、`Console.Out.Flush()`）。`--selftest` 16/16、build 0/0 |
| 2026-05-12 | `17844e0` | **`full_palette.nes` 跑通 → §7f(d) ✅** | Release build（`-c Release`）；`AprVisual.exe --screenshot full_palette --frames 48`：每 frame `714736` half-cycle（= 89342 dot，正確）；VBL 同步迴圈 **frame ~10-17 收斂**（CPU 脫離 `$805x` → `$8163/8168`、`A=30 X=3D Y=0A`）；之後 frame 長度 `714736`/`714728` 交替（= 開 BG 後奇數 frame 少 1 dot，跟真機一致）；frame 48 截圖 = **完整 64 色 palette 格子 + emphasis 染色**（`etc/fp_48.png`，已用 `tools/send_mail.py` 寄出）。邊緣有橫向拖影/位移（hpos→column 差 ~1 + 近似 master palette）→ 留給 (e)。`--selftest` 16/16、build 0/0 |
| 2026-05-12 | (本次) | `--benchmark` 實作 + S1 效能基線 | `TestRunner --benchmark <rom> [--frames N]`：headless 跑 N frame、量 wall-clock、印 **FPS**（模擬 NES frame/s）、**MIPS**（模擬 6502 cycle/s + 換算指令數）、raw step 率（master half-cycle/s）；之前 `--benchmark` 只是 placeholder。量到（Release、`full_palette` 24 frame、371 s）：**FPS ≈ 0.06**（≈ 0.1% realtime）、**≈ 1,900 模擬 6502 cycle/s（≈ 0.0019 MHz，0.1% of 1.79 MHz 2A03）**、inner loop ≈ **46k switch-level step/s**；compose+settle ≈ 0.36 s。記在 §「效能基準」（= S3 的 before 基線）。`--selftest` 16/16、build 0/0 |
| | | | |
